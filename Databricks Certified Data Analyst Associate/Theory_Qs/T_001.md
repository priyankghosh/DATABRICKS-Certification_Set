# T_001

#### Q1) In Databricks, what is the impact on a dashboard if the configured refresh rate for the dashboard is set to be more frequent than the "Auto Stop" setting of the SQL Warehouse?

a) **The dashboard will continue to refresh at the set interval, the SQL Warehouse will continue running.**
b) The warehouse automatically adjusts its "Auto Stop" setting to match the dashboard's refresh rate.
c) The dashboard will not be refreshed.
d) The dashboard will stop refreshing once the warehouse enters "Auto Stop" mode, potentially leading to outdated data being displayed.
e) The warehouse's "Auto Stop" setting is irrelevant, as the dashboard's refresh rate does not interact with warehouse settings.

***Overall explanation***
If a dashboard's refresh rate is set more frequently than the data warehouse's "Auto Stop" timing, the SQL Warehouse will continue running.

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q2) Consider the following table named *Employee*:

| EmployeeID | Name        | Department | Salary |
|------------|-------------|------------|--------|
| 1          | John Doe    | Marketing  | 50000  |
| 2          | Jane Smith  | Sales      | 55000  |
| 3          | Alice Jones | HR         | 45000  |
| 4          | Bob Brown   | IT         | 60000  |

**Given Output**:

| Name       | Department |
|------------|------------|
| Jane Smith | Sales      |
| Bob Brown  | IT         |

#### Which of the queries below could have been used to generate the output?

a) SELECT Department, Name FROM Employee WHERE Salary > 50000;
b) SELECT * FROM Employee WHERE Salary > 50000;
c) **SELECT Name, Department FROM Employee WHERE Salary > 50000;**
d) SELECT Name, Department FROM Employee WHERE Salary OVER 50000;
e) SELECT Name, Department FROM Employee WHERE Salary >= 50000;

***Overall explanation***
References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q3) What is the correct method to rename a table in Databricks?

a) Modify the table name in the metadata file.
b) Use the `RENAME TABLE` command.
c) Delete the old table and create a new one with the desired name.
d) **Use the `ALTER TABLE RENAME TO` command.**
e) Update the table name through the Databricks UI.

***Overall explanation***
In Databricks, the standard way to rename a table is by using the SQL command `ALTER TABLE [old_table_name] RENAME TO [new_table_name]`. This command changes the table's name in the metadata without affecting the actual data stored in the table. It's a straightforward and efficient method for renaming tables within Databricks environments.

References: [Link](https://docs.databricks.com/en/sql/language-manual/sql-ref-syntax-ddl-alter-table.html)

```
Domain
Data Management
```

<br />

#### Q4) A data analyst is using Databricks SQL to visualize data showing the monthly sales trends across different regions. To effectively communicate the data, the analyst needs to choose an appropriate visualization type. Based on the data and the goal of showing trends over time, which visualization type should the analyst select in Databricks SQL?

a) Bar chart, as it is best for comparing categories of data at a single point in time.
b) **Line chart, as it effectively displays trends and changes over time.**
c) Scatter plot, to show the relationship between sales volume and time.
d) Pie chart, to show the proportion of sales in each region compared to the whole.
e) Heatmap, for representing the intensity of sales in different regions.

***Overall explanation***
For visualizing trends over time, especially when dealing with time-series data like monthly sales, a line chart is often the most appropriate choice. It allows the audience to easily observe the changes and trends in the sales data over the specified time periods. This type of chart is effective in highlighting upward or downward trends and making comparisons between different time periods or regions.

References: [Link 1](https://docs.databricks.com/en/sql/user/visualizations/index.html) [Link 2](https://learn.microsoft.com/en-us/azure/databricks/visualizations/visualization-types)

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q5) In Azure Databricks, when using a table visualization type for SQL queries, which of the following statements accurately reflects its capabilities and limitations?

a) They are limited to displaying only numerical data and cannot handle textual or categorical data.
b) Table visualizations in Databricks are primarily used for external data export and are not suitable for in-dashboard data presentation.
c) **They allow for manual reordering, hiding, and formatting of data columns, but do not perform data aggregations within the result set.**
d) Table visualizations primarily function to display graphical representations like charts and graphs, rather than tabular data.
e) Table visualizations in Databricks automatically aggregate data within the result set, providing a summary view.

***Overall explanation***
Table visualizations in Azure Databricks provide a flexible way to present data in a structured table format. Users can manually reorder, hide, and format the data columns to suit their analysis needs. However, it's important to note that these visualizations do not perform any data aggregations within the result set. All necessary aggregations must be computed within the SQL query before visualization.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/visualizations/visualization-types#table)

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q6) In a Databricks SQL context, consider a dataset with columns *Department*, *Employee*, and *Sales*. You are required to analyze the data using the `ROLLUP` and `CUBE` functions. 
#### Given this scenario, select the correct statements regarding the type of aggregations `ROLLUP` and `CUBE` would generate when applied to the *Department* and *Employee* columns.

a) Neither `ROLLUP` nor `CUBE` will generate subtotals for individual 'Departments' or 'Employees'; they only provide a grand total.
b) **`CUBE` creates aggregations for all possible combinations of the columns in the `GROUP BY` clause. It would generate subtotals for each 'Department', each 'Employee', each combination of 'Department' and 'Employee', but not a grand total.**
c) Both `ROLLUP` and `CUBE` produce identical aggregations, including subtotals for each 'Department', each 'Employee', each combination of 'Department' and 'Employee', and a grand total.
d) ROLLUP generates aggregations in a random order, ignoring the hierarchy of columns in the GROUP BY clause. It produces subtotals only for individual employees, without generating department-level subtotals or a grand total.
e) **`ROLLUP` provides aggregations only for each combination of 'Department' and 'Employee', while `CUBE` gives a detailed breakdown including each 'Department', each 'Employee', and a grand total.**

***Overall explanation***
•	ROLLUP is used for hierarchical data aggregation. It starts with the most detailed level (in this case, 'Department' and 'Employee') and rolls up to broader levels ('Department'), ending with a grand total.
•	The CUBE function generates all possible combinations, including subtotals for each 'Department', each 'Employee', their combinations, and also a grand total.
•	The other options are not correct as they either misinterpret the functions of ROLLUP and CUBE or provide an incomplete or inaccurate description of the aggregations these functions generate.

Understanding the functionality of ROLLUP and CUBE is essential for SQL data analysis, particularly in platforms like Databricks, where complex data manipulation and aggregation are common tasks.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-qry-select-groupby)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q7) A data analyst is creating a dashboard in Databricks and wants to visualize sales trends using a line chart. They need to ensure the x-axis displays dates in a readable format while maintaining proper chronological order. Which approach should the analyst take?

a) Convert the date column to a string format before adding it to the visualization to ensure proper label readability.
b) Store the dates as integer values representing YYYYMMDD to ensure proper ordering on the x-axis.
c) Use the Databricks visualization settings to manually arrange the dates in the correct order after plotting the chart.
d) Apply a text-based sorting approach by converting the date to a categorical variable before adding it to the visualization.
e) **Ensure the date column is stored as a timestamp or date data type before using it in the visualization.**

***Overall explanation***
In Databricks, visualizations require the date column to be in a proper date or timestamp format for correct chronological ordering. If the date is stored as a string, the visualization may sort it alphabetically instead of by actual date order. Unlike spreadsheets, Databricks does not provide manual axis adjustments, so ensuring the correct data type is crucial. The other options are incorrect because they introduce potential sorting and formatting issues rather than solving them.

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q8) What are the primary responsibilities of a table owner in Databricks?

a) Ensuring the table is always available for querying and analysis.
b) **Managing user access and permissions for the table.**
c) Regularly updating the table data to keep it current.
d) Designing the visual representation of the table data.
e) Optimizing the table for faster query performance.

***Overall explanation***
The table owner in Databricks is primarily responsible for managing user access and permissions related to the table. This involves setting and adjusting who can view, modify, or delete the table, ensuring proper data governance and security. The owner's role is crucial in maintaining the integrity and confidentiality of the data contained within the table.

References: [Link](https://docs.databricks.com/en/data-governance/unity-catalog/manage-privileges/ownership.html)

```
Domain
Data Management
```

<br />

#### Q9) In a Databricks environment, you are optimizing the performance of a data processing task that involves complex operations on arrays within a Spark SQL dataset.
#### Which of the following higher-order functions in Spark SQL would be most suitable for efficiently transforming elements within an array column scores?

a) SELECT COLLECT_LIST(scores) FROM dataset GROUP BY scores;
b) SELECT ARRAY_SORT(scores) FROM dataset;
Your answer is correct
c) **SELECT TRANSFORM(scores, score -> score * 2) FROM dataset;**
d) SELECT ARRAY_CONTAINS(scores, 10) FROM dataset;
e) SELECT EXPLODE(scores) FROM dataset;

***Overall explanation***
The `TRANSFORM` function in Spark SQL is a higher-order function that allows for efficient manipulation of array elements. 
It applies a specified lambda function to each element of an array, enabling complex transformations within a single SQL query. This approach optimizes performance by minimizing the need for multiple operations and reducing data shuffling. Such functions are particularly useful in big data scenarios where data processing needs to be both efficient and scalable.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/optimizations/higher-order-lambda-functions)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q10) When sharing Databricks SQL dashboards, there are two primary settings: 'Run as viewer' and 'Run as owner'. What are the pros and cons of each setting in the context of sharing dashboards?

a) 'Run as viewer' and 'Run as owner' both provide the same level of data visibility (pro), but may have limitations in customizing query execution (con).
b) 'Run as owner' offers greater customization of dashboard settings (pro), but requires viewers to have advanced knowledge of SQL (con). 'Run as viewer' simplifies the user experience (pro), but may lead to inconsistent data reporting (con).
c) Both settings allow for easy sharing of dashboards (pro), but can lead to complexities in managing user permissions and data access (con).
d) **'Run as viewer' enhances data security by adhering to individual viewer's permissions (pro), but may limit data visibility (con). 'Run as owner' ensures consistent data visibility across users (pro), but might pose security risks if the owner has broader data access (con).**
e) 'Run as viewer' allows full customization of queries for each viewer (pro), but can be more resource-intensive (con). 'Run as owner' simplifies query management (pro), but doesn't account for individual user access levels (con).

***Overall explanation***
Each sharing setting in Databricks dashboards has its benefits and drawbacks. The 'Run as viewer' setting respects the individual access permissions of each viewer, providing a secure way to share data. However, this can limit the visibility of data based on those permissions. On the other hand, 'Run as owner' ensures that all viewers see the same data, which is useful for consistency, but this could expose sensitive information if the dashboard owner has access to a broader range of data than the viewers.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/security/auth-authz/access-control/dashboard/-acl)

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q11) When importing data from an Amazon S3 bucket into a Databricks environment using Databricks SQL, which SQL command is typically used to perform this operation?

a) INSERT INTO my_table SELECT * FROM s3a://mybucket/mydata.csv;
b) SELECT * INTO my_table FROM OPENROWSET(BULK 's3://mybucket/mydata.csv', SINGLE_CLOB) AS mydata;
c) **COPY INTO my_table FROM 's3://mybucket/mydata.csv' FILEFORMAT = CSV;**
d) CREATE TABLE my_table USING CSV LOCATION 's3://mybucket/mydata.csv';
e) LOAD DATA INPATH 's3://mybucket/mydata.csv' INTO TABLE my_table;

***Overall explanation***
The COPY INTO command is often used for this purpose, as it is well-suited for incremental and bulk data loading in Databricks SQL.

References: [Link](https://docs.databricks.com/en/ingestion/copy-into/tutorial-dbsql.html)

```
Domain
Databricks SQL
```

<br />

#### Q12) In the context of Unity Catalog in Databricks, what is the purpose of specifying a MANAGED LOCATION when creating a catalog or schema?

a) To determine the default file format for the catalog or schema.
b) To set the encryption type for data stored in the catalog or schema.
c) To specify the computational resources for processing data in the catalog or schema.
d) To configure the network access settings for the catalog or schema.
e) **To define the cloud object storage location for data of managed tables and volumes.**

***Overall explanation***
The `MANAGED LOCATION` keyword is used to specify a location in cloud object storage for storing data for managed tables and managed volumes in Unity Catalog. This feature allows for the organization and governance of data at the catalog or schema level, ensuring data isolation and efficient data management.

References: [Link](https://docs.databricks.com/en/connect/unity-catalog/managed-storage.html)

```
Domain
Data Management
```

<br />

#### Q13) Which of the following best describes the key audience and side audiences for Databricks SQL? Select the most appropriate option.

a) The key audience is data scientists, with both data engineers and software developers as side audiences.
b) The key audience is exclusively data engineers, with software developers as a side audience.
c) The main audience comprises business intelligence professionals, with data analysts and data engineers as side audiences.
d) The key audience includes both data analysts and data scientists, with no significant side audiences.
e) **The key audience includes both data analysts and data scientists, with no significant side audiences.**

***Overall explanation***
- The key audience for Databricks SQL primarily includes data analysts, while side audiences encompass data scientists and business intelligence professionals. Databricks SQL is a powerful SQL analytics platform that enables these professionals to create and share insights efficiently. Data analysts, who form the core audience, can connect business intelligence tools to SQL endpoints or use built-in analytics capabilities like the SQL query editor, visualizations, and dashboards. This flexibility allows for a broad range of analytics and reporting activities directly on the data lake.

- Data scientists also benefit from Databricks SQL, especially when working with the SQL editor, which is a part of a dedicated SQL-native workspace designed for data analysts. This workspace simplifies data exploration and query execution, allowing data scientists to work from the same data source as data analysts, thereby maintaining a single source of truth and reducing infrastructure complexity.

- Additionally, business intelligence professionals, who are part of the side audience, can leverage built-in connectors to common BI tools for production analytics. Databricks SQL supports integration with major BI tools like Tableau and Microsoft Power BI, enabling these professionals to query the freshest and most complete data in the data lake.

```
Domain
Databricks SQL
```

<br />

#### Q14) In Databricks, setting up a refresh schedule for dashboards is essential for ensuring that the displayed data is current. How does one configure a refresh schedule for a Databricks SQL dashboard?

a) **Click Schedule at the upper-right of the dashboard. Then, click Add schedule.**
b) Manually updating the dashboard at regular intervals without any automated scheduling.
c) Writing a custom script in the dashboard's SQL queries to automate refreshes.
Sending periodic requests to the Databricks support team to refresh the dashboard.
d) Utilizing an external tool to trigger refreshes in the Databricks dashboard.

***Overall explanation***
A dashboard’s owner and users with the **Can Edit** permission can configure a dashboard to automatically refresh on a schedule.

References: [Link](https://docs.databricks.com/en/sql/user/dashboards/index.html#automatically-refresh-a-dashboard)

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q15) You are analyzing retail sales data in Databricks SQL with the columns *Store*, *Product*, and *Revenue*. You decide to use `ROLLUP`, `CUBE`, and `GROUPING SETS` to generate different levels of aggregations.

#### Which of the following statements is correct when applying these functions to (‘Store’, ‘Product’)?

a) **`ROLLUP(Store, Product)` produces subtotals for each Store, each (Store, Product) combination, and a grand total, but does not generate subtotals for individual Products across all Stores.**
b) `CUBE(Store, Product)` generates only two levels of aggregation: one for each unique (Store, Product) pair and another for the grand total.
c) `GROUPING SETS((Store, Product), (Store), (Product), ())` is functionally equivalent to `CUBE(Store, Product)`.
d) `ROLLUP(Store, Product)` and `CUBE(Store, Product)` produce the same aggregations because both create subtotals for each Store, each Product, and a grand total.
e) **`CUBE(Store, Product)` generates all possible subtotal combinations, including totals for each Store, each Product, each (Store, Product) combination, and a grand total.**

***Overall explanation***
- ROLLUP(Store, Product) generates a subtotal for each Store, each (Store, Product) combination, and a grand total, but does not create subtotals for each Product across all Stores. This is because ROLLUP follows a hierarchical aggregation pattern where it first aggregates at the most granular level and then moves up, but it does not produce independent subtotals for each Product across all Stores.

- CUBE(Store, Product) produces all possible subtotal combinations, including totals for each Store, each Product, each (Store, Product) combination, and a grand total. Unlike ROLLUP, CUBE does not assume a hierarchy and instead creates every possible aggregation level, making it more comprehensive when all subtotal possibilities are required.

- The other options are incorrect for various reasons. Option 2 is wrong because CUBE generates four levels of aggregation, not just two. Option 3 is incorrect because GROUPING SETS((Store, Product), (Store), (Product), ()) does not perfectly match the behavior of CUBE, even though it can be used to achieve similar results with manual specification. Option 4 is incorrect because ROLLUP does not generate subtotals for each Product across all Stores, while CUBE does.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-qry-select-groupby)

```
Domain
SQL in the Lakehouse
```

<br />


#### Q16) In a Databricks SQL environment, you are tasked with analyzing sales data. The table SalesRecords contains columns SaleID, ProductID, SaleAmount, and SaleDate.
#### You need to find all products that had their first sale amounting to more than $500.
#### Which SQL query using a subquery appropriately retrieves this information?

a) **SELECT ProductID FROM SalesRecords WHERE SaleAmount > 500 AND SaleDate = (SELECT MIN(SaleDate) FROM SalesRecords WHERE ProductID = SalesRecords.ProductID);**
b) SELECT DISTINCT ProductID FROM SalesRecords WHERE SaleAmount > 500 GROUP BY ProductID HAVING SaleDate = MIN(SaleDate);
c) SELECT ProductID FROM SalesRecords WHERE SaleAmount > 500 AND SaleDate = (SELECT MIN(SaleDate) FROM SalesRecords GROUP BY ProductID);
d) SELECT ProductID FROM SalesRecords WHERE SaleAmount > 500 AND SaleID IN (SELECT MIN(SaleID) FROM SalesRecords GROUP BY ProductID);
e) SELECT ProductID FROM (SELECT ProductID, MIN(SaleDate) AS FirstSaleDate FROM SalesRecords GROUP BY ProductID) AS FirstSales WHERE FirstSaleDate > 500;

***Overall explanation***
References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-qry-query)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q17) A data analyst is working with a database in Databricks and needs to update a table named SalesData.
#### The analyst has a new batch of data that includes some records already present in SalesData and some new records. They must decide between using `MERGE INTO`, `INSERT INTO`, and `COPY INTO` commands.
#### Considering the functions and typical use cases of these commands, which of the following statements is true?

a) MERGE INTO and INSERT INTO perform the same functions, and COPY INTO is not a recognized command in Databricks.
b) INSERT INTO and COPY INTO are both used for inserting new records, but COPY INTO is specifically for loading data from external sources, and MERGE INTO is for updating existing records only.
c) COPY INTO is used for updating existing records and inserting new records, MERGE INTO is only for inserting new records, and INSERT INTO is not used in Databricks.
d) **MERGE INTO is suitable for updating existing records and inserting new records, while INSERT INTO is used only for adding new records, and COPY INTO is used for loading data from files.**
e) INSERT INTO can be used for both updating existing records and inserting new records, while MERGE INTO is only for inserting new records, and COPY INTO is not used in Databricks.

***Overall explanation***
`MERGE INTO` is suitable for both updating existing records and inserting new records depending on whether a match is found in the target table. `INSERT INTO` is typically used to add new records to a table and does not update existing records. `COPY INTO` is a specialized command used in Databricks for loading data into a table from external sources such as files in a file system.
- `MERGE INTO` is used for complex operations where you need to update existing records or insert new ones based on some matching condition.
- `INSERT INTO` adds new rows to a table and is straightforward for adding new data.
- `COPY INTO` is specifically designed for loading data from files into a table, useful when importing data from external sources.

References: [Link 1](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/delta-merge-into) [Link 2](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-dml-insert-into) [Link 3](https://learn.microsoft.com/en-us/azure/databricks/ingestion/copy-into/)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q18) In the context of Databricks, how is Personally Identifiable Information (PII) typically handled to ensure data privacy and compliance?

a) **Through the use of Delta Lake features for fine-grained access control.**
By anonymizing PII data through built-in Databricks functions.
PII is not specifically handled in Databricks; it relies on external tools.
By creating a separate database for PII.
By automatically encrypting all data fields that contain PII.

***Overall explanation***
Databricks, especially with the integration of Delta Lake, provides mechanisms for handling PII, such as fine-grained access control. This allows for specific permissions on sensitive data fields, ensuring that only authorized users can access PII. It's a crucial aspect of maintaining data privacy and compliance with regulations like GDPR and HIPAA in data processing and analytics workflows.

References: [Link](https://www.databricks.com/blog/2020/11/20/enforcing-column-level-encryption-and-avoiding-data-duplication-with-pii.html)

```
Domain
Data Management
```

<br />

#### Q19) Which of the following is a critical organization-specific consideration when handling PII data?

a) Prioritizing cost-saving measures over data security for PII data.
b) Implementing a one-size-fits-all approach to PII data storage and processing.
c) Developing a uniform public access policy for all PII data.
d) Always using the same encryption method for PII data across all departments.
e) **Adapting PII data handling protocols to comply with regional and sector-specific privacy laws.**

***Overall explanation***
When managing PII data, it is essential for organizations to consider the legal and regulatory environment they operate in. This often requires adapting data handling protocols to comply with various regional and sector-specific privacy laws. Unlike options A, B, C, and E, which suggest uniform or generalized approaches, adapting to specific legal requirements ensures both compliance and the security of sensitive data. This adaptation may involve different encryption methods, storage solutions, and access policies depending on the jurisdiction and industry of the organization.

References: [Link](https://www.digitalguardian.com/blog/pii-data-classification-4-best-practices)

```
Domain
Data Management
```

<br />

#### Q20) When integrating Databricks SQL with popular visualization tools such as Tableau, Power BI, and Looker, which of the following steps is commonly involved in the connection process?

a) Using email to transfer data snapshots from Databricks SQL to the visualization tools.
b) Requiring a third-party data integration tool to mediate the connection between Databricks SQL and the visualization tools.
c) Manually exporting data from Databricks SQL to CSV files and importing them into the visualization tool.
d) **Setting up a direct JDBC/ODBC connection between Databricks SQL and the visualization tool.**
e) Implementing a custom API for each visualization tool to query data from Databricks SQL.

***Overall explanation***
Connecting Databricks SQL to visualization tools like Tableau, Power BI, and Looker generally involves setting up a direct JDBC/ODBC connection. Here's a brief overview of how this is typically done for each tool:
1.  **Tableau**:
    - To connect Tableau to Databricks, you start Tableau and select Databricks under 'Connect'.
    - You then enter the Server Hostname, HTTP Path, and choose your authentication method (Azure Active Directory via OAuth, Personal Access Token, or Username / Password).
    - After entering the sign-in credentials, you can start your analysis by selecting and dragging the table to the canvas in Tableau.
2.  **Power BI**:
    - Power BI has an enhanced Azure Databricks connector integrated into Power BI Desktop and Power BI Service.
    - You can select Databricks as a data source in Power BI Desktop, authenticate using Azure Active Directory (AAD) credentials, and enter Databricks-specific connection details.
    - This setup supports DirectQuery, allowing users to directly access and query data in Databricks while enforcing data lake security controls, without the need to duplicate security controls in Power BI.
3.	Looker:
    - The process of connecting Looker to Databricks would typically require a cluster or SQL warehouse in your Databricks workspace.
    - You need to gather connection details like the Server Hostname, Port, and HTTP Path.
    - The actual steps for configuring the connection would depend on the specific Looker version and the setup of your Databricks environment. The connection often involves configuring the JDBC/ODBC settings in Looker to match those provided by Databricks.

In each case, the key to successful integration is the correct configuration of the JDBC/ODBC connection details and the appropriate authentication method. This ensures seamless data transfer and interaction between Databricks SQL and the visualization tool.

References: [Link 1](https://www.databricks.com/blog/2020/10/30/announcing-azure-databricks-power-bi-connector-public-preview.html) [Link 2](https://docs.databricks.com/en/partners/bi/tableau.html) [Link 3](https://docs.databricks.com/en/partners/bi/looker.html)

```
Domain
Databricks SQL
```

<br />

#### Q21) In Databricks SQL, what is the primary purpose of using the ANALYZE TABLE command?

a) To back up the table data to a specified location.
b) **To collect statistics about the table for optimizing query performance.**
c) To change the data storage format of the table.
d) To modify the structure of a table by adding or removing columns.
e) To synchronize the table data with an external data source.

***Overall explanation***
The ANALYZE TABLE command in Databricks SQL is used to gather important statistics about a table. These statistics, including details like the number of rows and size in bytes, are crucial for the query optimizer in creating efficient query plans. By understanding the distribution and size of the data in a table, Databricks SQL can make more informed decisions about how to execute queries, leading to improved performance. The command does not alter the table structure, back up data, change storage formats, or synchronize data with external sources. It's primarily focused on collecting data metrics for internal optimization purposes.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-aux-analyze-table)

```
Domain
Analytics applications
```

<br />

#### Q22) Which of the following statements accurately distinguishes between discrete and continuous statistics in the context of data analysis?

a) Continuous data can only take on integer values, whereas discrete data can take on any value, including decimals.
b) Continuous data is always non-numeric, whereas discrete data is numeric.
c) **Discrete data can only take on a finite number of values, while continuous data can take on any value within a given range.**
d) Discrete data is used for time series analysis, while continuous data is not suitable for this purpose.
e) Both discrete and continuous data are types of categorical data used in qualitative analysis.

***Overall explanation***
- In the context of data analysis, especially relevant for the Databricks Data Analyst Associate Certification Exam, it's crucial to understand the distinction between discrete and continuous data as they have different applications and implications in statistical analysis.

- Discrete data are quantitative and can take only fixed, distinct values. These values are countable, but not necessarily whole numbers. For example, shoe sizes can be 7, 7.5, 8, etc., but you won't find a shoe size of 7.7. They are characterized by clear gaps between values and are generally less precise due to their distinct nature. Discrete data are often visualized using bar charts, pie charts, or scatter plots. Examples of discrete data include the number of students in a class, cars in a parking lot, or days in a month with temperatures above a certain degree.

- On the other hand, continuous data can assume any value within a range and can be meaningfully divided into smaller parts, including fractional and decimal values. They represent measurements and can be infinitely precise, as they can potentially be measured to an ever-increasing degree of accuracy. Continuous data are typically represented using histograms or line charts, ideal for showcasing trends or patterns over time. Common examples include temperature, weight, height, and the time it takes for runners to complete a race.

References: [Link](https://statisticsbyjim.com/basics/discrete-vs-continuous-data/)

```
Domain
Analytics applications
```

<br />

#### Q23) Consider the following *SalesData* table:

| Region | Product | SalesAmount |
|--------|---------|-------------|
| North  | A       | 100         |
| South  | B       | 200         |
| East   | C       | 150         |
| West   | D       | 300         |

#### An SQL query is executed, resulting in the following output:

| Region | Product | SalesAmount |
|--------|---------|-------------|
| North  | A       | 100         |
| South  | B       | 200         |
| East   | C       | 150         |
| West   | D       | 300         |
| North  | A       | 100         |
| South  | B       | 200         |
| East   | C       | 150         |
| West   | D       | 300         |
| West   | D       | 300         |

#### Which query was most likely used to generate this output?

a) SELECT Region, Product, SUM(SalesAmount) AS TotalSales FROM SalesData GROUP BY CUBE(Region, Product);
b) SELECT Region, Product, SUM(SalesAmount) AS TotalSales FROM SalesData GROUP BY GROUPING SETS ((Region, Product), (Region), ());
c) SELECT Region, Product, SUM(SalesAmount) AS TotalSales FROM SalesData GROUP BY Region, Product;
d) SELECT Region, Product, COUNT(*) AS TotalSales FROM SalesData GROUP BY Region, Product WITH ROLLUP;
e) **SELECT Region, Product, SUM(SalesAmount) AS TotalSales FROM SalesData GROUP BY ROLLUP(Region, Product);**

***Overall explanation***

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/functions/cube)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q24) You are using Databricks to load data from a CSV file located in DBFS (dbfs:/data/sales.csv) into a Delta table named SalesData.
#### The CSV file has a header row and fields separated by commas. Which Databricks SQL COPY INTO command correctly imports this data?

a) **COPY INTO SalesData FROM 'dbfs:/data/sales.csv' FILEFORMAT = CSV FORMAT_OPTIONS ('header' = 'true');**
b) COPY INTO SalesData FROM 'dbfs:/data/sales.csv' USING FORMAT AS CSV HEADER = TRUE;
c) COPY INTO SalesData FROM 'dbfs:/data/sales.csv' FILEFORMAT = 'CSV';
d) COPY INTO SalesData FROM 'dbfs:/data/sales.csv' USING (FILEFORMAT = CSV, HEADER = 'true');
e) COPY INTO SalesData FROM CSV 'dbfs:/data/sales.csv' WITH HEADER;

***Overall explanation***
This question is designed to test knowledge of the COPY INTO statement in Databricks for importing data from cloud storage into a Delta table, a common task in data integration and ETL processes.

References: [Link 1](https://learn.microsoft.com/en-us/azure/databricks/ingestion/copy-into/) [Link 2](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/delta-copy-into)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q25) In Databricks, how does the persistence of data differ between a view and a temporary view?

a) A view is session-specific, while a temporary view persists across sessions.
b) A view stores data physically, whereas a temporary view only exists during the session.
c) Temporary views allow data modifications, unlike regular views.
d) **Both views and temporary views do not store data physically, but a view persists beyond the session.**
e) Both views and temporary views store data physically in the workspace.

***Overall explanation***
In Databricks, views and temporary views do not store data physically; they are essentially saved queries. The key difference lies in their persistence: a regular view is a permanent database object and persists across different sessions, whereas a temporary view is session-specific and disappears once the session ends. This difference is crucial for understanding the lifecycle and scope of data representation in Databricks.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/lakehouse/data-objects)

```
Domain
Data Management
```

<br />


#### Q26) Which of the following statements best describes the function of Databricks SQL queries in the Databricks platform?

a) Databricks SQL queries are primarily used for configuring cluster settings and managing user permissions.
b) Databricks SQL queries are a feature for visualizing data but do not support writing or running SQL code.
c) **Databricks SQL queries provide a dedicated environment to write, test, and run SQL code for data analysis and processing.**
d) Databricks SQL queries are designed to write and execute machine learning models only, and not for general SQL code execution.
e) Databricks SQL queries are used exclusively for scheduling jobs and automating workflows, without any SQL code execution capabilities.

***Overall explanation***
- Databricks SQL queries provide a versatile environment within the Databricks platform for writing and executing SQL code. This functionality is crucial for performing various data-driven tasks, as it allows users to define and run queries against tables or other data sources to gain insights.

- In Databricks, you can create, organize, and execute SQL queries using the SQL editor. This tool is designed to facilitate easy and efficient SQL query construction and execution. The SQL editor supports features like autocomplete for efficient coding and the ability to view and organize queries. Users can create new queries using the SQL editor, and the platform allows for the organization of these queries into folders along with other Databricks objects.

- Queries in Databricks can target both data in the Databricks lakehouse and external data sources. The platform supports querying data in multiple formats and from various enterprise systems, making it a flexible solution for a wide range of data analysis tasks. You can query data interactively using not only the SQL editor but also notebooks, file editor, and dashboards. Additionally, Databricks supports running queries as part of Delta Live Tables pipelines or workflows.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/user/queries/queries)

```
Domain
Databricks SQL
```

<br />

#### Q27) You have a table customer_orders in Databricks SQL with a column order_info in JSON format. This column includes a nested array items, where each element has product_id (string) and quantity (integer).
#### Which of the following queries returns a single row with two columns—product_id and quantity—for the product with product_id equal to “X100”?

**Syntax used to create the table *CustomerOrders*:**

```
CREATE TABLE customer_orders AS 
SELECT 
'{ 
    "items": [ 
                {"product_id": "X100", "quantity": 3}, 
                {"product_id": "Y200", "quantity": 1} 
            ] 
}' as order_info;
```

a) **SELECT order_info:items[0].product_id AS product_id, order_info:items[0].quantity AS quantity FROM customer_orders;**
b) SELECT order_info:items.product_id, order_info:items.quantity FROM customer_orders;
c) SELECT EXPLODE(order_info:items) AS (product_id, quantity) FROM customer_orders;
d) SELECT FLATTEN(order_info:items.product_id[0], order_info:items.quantity[0]) FROM customer_orders;
e) SELECT order_info:items[*].product_id, order_info:items[*].quantity FROM customer_orders UNNEST items;

***Overall explanation***
References: [Link](https://docs.databricks.com/en/optimizations/semi-structured.html)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q28) In the context of statistics, what are key moments of a statistical distribution?

a) The skewness and kurtosis, which describe the shape and tail behavior of the distribution.
b) The range, interquartile range, and standard deviation, which describe the variability of the distribution.
c) **The mean, variance, skewness, and kurtosis, which are the first four moments of a distribution.**
d) The maximum and minimum values, which set the boundaries of the distribution.
e) The mean, median, and mode, which define the central tendency of the distribution.

***Overall explanation***
Key moments of a statistical distribution include the mean (first moment), variance (second moment), skewness (third moment), and kurtosis (fourth moment). These moments are crucial in describing the characteristics of a distribution. The mean measures the central tendency, variance measures the dispersion, skewness indicates the asymmetry, and kurtosis describes the 'tailedness' of the distribution. Understanding these moments is essential for comprehensively describing and analyzing the behavior of data in a statistical context.

References: [Link](https://www.analyticsvidhya.com/blog/2022/01/moments-a-must-known-statistical-concept-for-data-science/)

```
Domain
Analytics applications
```

<br />

#### Q29) As a data analyst, you are preparing to present your findings from a recent study. To effectively communicate these insights, you decide to use Databricks SQL. Which of the following steps aligns most closely with the initial phase of preparing your presentation in Databricks SQL?

a) Drafting a preliminary report in a word processing software.
b) Developing a Python script to perform advanced statistical analysis.
c) **Crafting a SQL query to gather and summarize the relevant data.**
d) Designing an interactive frontend interface using HTML and CSS.
e) Scheduling meetings with stakeholders to discuss data interpretations.

***Overall explanation***
The initial phase in preparing a data-driven presentation in Databricks SQL typically involves crafting a SQL query to gather and summarize the relevant data. This step is crucial as it forms the foundation upon which the dashboard will be built, allowing you to extract meaningful insights from the dataset. The subsequent steps would include visualizing this data and arranging it on a dashboard for an effective presentation.

References: [Link](https://docs.databricks.com/en/sql/get-started/index.html)

```
Domain
Databricks SQL
```

<br />

#### Q30) Regarding the accessibility and functionality of Databricks SQL dashboards, which statement best reflects their use in a business environment by various stakeholders?

a) **A variety of users, including analysts, managers, and other stakeholders, can view and run Databricks SQL dashboards for collaborative data analysis and decision-making.**
b) Only the original creator of a Databricks SQL dashboard has the ability to view and run it, making it unsuitable for team collaboration.
c) Only data engineers and IT professionals can view and run Databricks SQL dashboards due to their technical nature.
d) Databricks SQL dashboards are exclusively for top-level executives for strategic decision-making and are not accessible to other users.
e) Databricks SQL dashboards are publicly accessible and can be viewed and run by anyone with internet access, without any restrictions.

***Overall explanation***
Databricks SQL dashboards are designed to be versatile and user-friendly, enabling a wide range of users within an organization to view and run them. This inclusivity makes the dashboards an ideal tool for collaborative data analysis and decision-making. Analysts, managers, and various other stakeholders can leverage these dashboards to gain insights from data, irrespective of their technical expertise. This accessibility facilitates better communication and collaboration across different departments and levels within an organization, enhancing the overall data-driven decision-making process.

References: [Link](https://docs.databricks.com/en/security/auth-authz/access-control/dashboard-acl.html)

```
Domain
Databricks SQL
```


<br />


#### Q31) Given a database with a table *Orders* containing columns *OrderId*, *CustomerId*, *OrderDate*, and *Amount*, you want to retrieve the list of orders placed by a particular customer, identified by CustomerId = 123, where the order amount is greater than $500.
#### Which SQL query will correctly retrieve this data?

a) **SELECT * FROM Orders WHERE CustomerId = 123 AND Amount > 500;**
b) DELETE FROM Orders WHERE CustomerId = 123 AND Amount > 500;
c) SELECT OrderId FROM Orders WHERE CustomerId = 123 OR Amount > 500;
d) SELECT CustomerId, Amount FROM Orders WHERE OrderId = 123 AND Amount > 500;
e) UPDATE Orders SET Amount = 500 WHERE CustomerId = 123;

***Overall explanation***
The correct answer, `SELECT * FROM Orders WHERE CustomerId = 123 AND Amount > 500`, effectively identifies the query that meets the specified conditions. This SQL query retrieves all records from the 'Orders' table where the 'CustomerId' is 123 and the 'Amount' is greater than $500. The use of the AND operator ensures both conditions must be met simultaneously, thereby filtering the orders specifically for the given customer and the minimum order amount criterion.

```
Domain
SQL in the Lakehouse
```

<br />

#### Q32) What is the primary purpose of Databricks SQL endpoints/warehouses in a data analytics environment?

a) To act as the primary interface for application development and deployment within the Databricks ecosystem.
b) To offer a centralized platform for advanced machine learning model development and deployment.
c) To provide a secure environment for data encryption and compliance management.
d) To serve as the primary storage location for large-scale data sets, replacing traditional data warehouses.
e) **To facilitate SQL-based data querying and analysis, offering a managed environment for running SQL queries on large datasets.**

***Overall explanation***
- SQL Warehouses in Databricks are designed to be powerful compute resources that enable users to execute SQL commands on data objects within Databricks SQL. These warehouses serve as infrastructure resources providing processing capabilities in the cloud, thus connecting users to the compute and data required to run their SQL queries efficiently.

- Databricks SQL supports different types of SQL warehouses, such as Serverless, Pro, and Classic, each offering varying performance capabilities. For instance, the Serverless SQL warehouse provides rapid startup and scaling features, making it highly responsive to varying query demands. It's particularly well-suited for ETL, business intelligence, and exploratory analysis. The Pro and Classic types also support various performance features but with different levels of efficiency and response times.

- SQL Warehouses in Databricks can be integrated with third-party BI and visualization tools, enhancing their usability in diverse data analytics environments. These warehouses are created and managed through a web UI or an API, providing flexibility in configuration and use.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/compute/sql-warehouse)

```
Domain
Databricks SQL
```

<br />

#### Q33) A data analyst is creating a dashboard to present monthly sales data to company executives.
#### The initial version of the dashboard contains accurate data but receives feedback that it is hard to interpret. The analyst then revises the dashboard by adjusting colors for better contrast, using consistent and clear fonts, and organizing charts logically.
#### After these changes, the executives find the dashboard much more informative and easier to understand. This scenario illustrates which of the following points about visualization formatting?

a) **Proper formatting can enhance readability and comprehension, leading to a more accurate interpretation of the data.**
b) Formatting is primarily used to reduce the size of the data set visually displayed.
c) Formatting changes the underlying data, thus altering the data's interpretation.
d) Formatting only affects the aesthetic aspect of the visualization and has no impact on its reception.
e) Over-formatting can lead to data misinterpretation by introducing visual biases.

***Overall explanation*** 
In this scenario, the changes made by the analyst - adjusting colors for better contrast, using consistent and clear fonts, and logically organizing charts - demonstrate how formatting can significantly impact the reception of a visualization. Proper formatting does not alter the underlying data but enhances its presentation, making it easier to read and understand. This leads to a more accurate and effective interpretation of the data, which is crucial in a business setting where decisions are often based on such visualizations.

References:
- [Tableau: Vizible Difference - Formatting Tips](https://www.tableau.com/about/blog/examining-data-viz-rules)
- [IBM - The Principles of Good Visualization](https://www.ibm.com/cloud/learn/data-visualization)
- [Forbes: Why Data Visualization Is Important For Business Leaders](https://www.forbes.com/sites/forbestechcouncil/2020/05/18/why-data-visualization-is-important-for-business-leaders/)

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q34) You are analyzing a dataset *OrderDetails* in a Databricks SQL environment, which includes *OrderID*, *ProductID*, *Quantity*, and *UnitPrice*.
#### Your objective is to find the total revenue generated by each product.
#### Which SQL query effectively aggregates this data to provide the desired output?

a) SELECT ProductID, SUM(Quantity) FROM OrderDetails GROUP BY ProductID;
b) SELECT ProductID, AVG(UnitPrice * Quantity) FROM OrderDetails GROUP BY ProductID;
c) SELECT OrderID, SUM(UnitPrice * Quantity) AS TotalRevenue FROM OrderDetails GROUP BY OrderID;
d) SELECT ProductID, SUM(UnitPrice) FROM OrderDetails GROUP BY ProductID;
e) **SELECT ProductID, SUM(UnitPrice * Quantity) AS TotalRevenue FROM OrderDetails GROUP BY ProductID;**

***Overall explanation***
References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-qry-select-groupby)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q35) How can you determine if a table in Databricks is managed or unmanaged?

a) By the type of data stored in the table.
b) By the speed of query execution on the table.
c) By the number of columns in the table.
d) By the size of the table data.
e) **By the location of the data files specified in the table definition.**

***Overall explanation***
The key difference between managed and unmanaged tables in Databricks lies in the location of their data files. A managed table means Databricks manages both the data and the metadata, and the data is stored in a default warehouse directory. For an unmanaged table, the user specifies the data file location in the table definition, and Databricks only manages the metadata. Checking this location in the table definition can reveal whether a table is managed or unmanaged.

References: [Link](https://docs.databricks.com/en/lakehouse/data-objects.html)

```
Domain
Data Management
```

<br />

#### Q36) In the context of Databricks SQL, which of the following statements accurately describes both a caution and a benefit of working with streaming data?

- a) Benefit: Streaming data can handle large volumes of data efficiently.
    - Caution: Real-time data processing may lead to higher error rates if not managed correctly.
- b) Benefit: Streaming data ensures complete data privacy.
    - Caution: It can lead to delayed data processing.
- c) Benefit: Streaming data simplifies data transformation.
    - Caution: It can lead to increased costs due to the need for more computing resources.
- d) Benefit: Streaming data reduces the need for storage.
    - Caution: It requires more complex SQL queries.
- e) **Benefit: Streaming data allows for real-time analytics and decision-making.**
    - **Caution: There is a higher risk of data inconsistency due to the continuous flow of data.**

***Overall explanation***
- Benefit: One of the primary benefits of working with streaming data in a Databricks SQL environment is the ability to perform real-time analytics. This feature is particularly useful in scenarios where immediate data analysis is crucial, such as monitoring financial transactions, tracking website activity, or analyzing sensor data in real-time.
- Caution: The continuous flow of data in a streaming context poses a challenge in maintaining data consistency. Unlike batch processing, where data is processed in chunks and consistency can be more easily managed, streaming data is susceptible to anomalies and inconsistencies due to its nature. This requires robust error handling and data validation mechanisms to ensure data integrity.

References: [Link](https://docs.databricks.com/en/structured-streaming/index.html)

```
Domain
Databricks SQL
```

<br />

#### Q37) How is data enhancement commonly applied in analytics?

a) Through cleaning and normalizing data to maintain consistency in databases.
b) By reducing the size of the dataset to improve processing speed.
c) **By incorporating external data sources to enrich existing datasets for deeper insights.**
d) By anonymizing sensitive information in datasets for privacy compliance.
e) By converting unstructured data into structured format for easier database storage.

***Overall explanation***
Data enhancement in analytics typically involves the process of augmenting existing datasets with additional information from external sources. This practice is aimed at providing more comprehensive insights and improving the accuracy of data analysis. By integrating supplementary data, analysts can gain a fuller understanding of the subject matter, leading to more informed decision-making and strategy development. This could include adding demographic information to customer data, incorporating geographic data for location-based analysis, or including social media data for sentiment analysis.

```
Domain
Analytics applications
```

<br />

#### Q38) What are the essential steps to execute a basic SQL query in Databricks?

a) Manually enter data into Databricks tables, write a SQL query in a text file, and use an external tool to execute the query.
b) **Open SQL Editor, select a SQL warehouse, specify the query, run the query.**
c) Create a data frame in Python or Scala, apply a SQL query to the data frame, and display the results.
d) Write a SQL query in a Databricks notebook, validate the syntax, execute the query, and view the results.
e) Import data into a Databricks dataset, use a BI tool to run the SQL query, and export the results to a CSV file.

***Overall explanation***
To execute a basic SQL query in Databricks, the typical process involves using the Databricks SQL Editor interface. Here, you write the SQL query, select the appropriate data source or table you want to query against, run the query, and then view or visualize the results directly within the interface. This process allows for a seamless experience in querying and analyzing data using SQL within the Databricks environment.

References: [Link 1](https://docs.databricks.com/en/sql/user/queries/queries.html) [Link 2](https://docs.databricks.com/en/sql/get-started/index.html)

```
Domain
Databricks SQL
```

<br />

#### Q39) What are the primary benefits of implementing Delta Lake within the Databricks Lakehouse architecture?

a) Delta Lake is beneficial only for handling unstructured data types.
b) It mainly improves the graphical user interface for data exploration.
c) **Delta Lake provides ACID transactions, scalable metadata handling, and time-travel features.**
d) Delta Lake primarily enhances data security and compliance features.
e) It offers high-speed streaming data ingestion and real-time analytics capabilities.

***Overall explanation***
Delta Lake brings significant benefits to the Lakehouse architecture in Databricks by offering ACID transactions to ensure data integrity, scalable metadata handling for large datasets, and time-travel capabilities allowing users to access historical data. These features enable more robust data management, improved data reliability, and enhanced analytical capabilities, making Delta Lake a powerful component of the Lakehouse architecture.

References: [Link](https://www.databricks.com/product/delta-lake-on-databricks)

```
Domain
Data Management
```

<br />

#### Q40) In the context of Delta Lake tables in Databricks, how is historical data maintained, and what command is used to access this history?

a) Delta Lake tables do not maintain historical data.
b) Historical data is maintained in separate snapshot tables, accessed with the `SHOW SNAPSHOT` command.
c) **Historical data is maintained through versioned table updates, accessed with the `DESCRIBE HISTORY` command.**
d) Historical data is stored in a dedicated audit log, accessed with the `VIEW AUDIT LOG` command.
e) Historical data is maintained through periodic backups, accessed with the `SHOW BACKUP` command.

***Overall explanation***
Reasoning: Delta Lake tables in Databricks maintain historical data by creating a new table version for each operation that modifies the table. This allows for auditing, rollback, and querying specific versions through time travel. The `DESCRIBE HISTORY` command is used to retrieve information about each write operation to a Delta table, showing the version, timestamp, user, and other details of the operation.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/delta/history)

```
Domain
Data Management
```

<br />

#### Q41) In a Databricks Lakehouse, you are working with silver-level data which involves refined, cleansed, and transformed datasets.
You have a silver table named CustomerInteractions that needs deduplication based on the customer_id and interaction_date columns.
The requirement is to retain only the latest interaction for each customer on any given date. Which approach would be the most efficient for cleaning this data in Databricks?

a) Create a new table from `CustomerInteractions` using `GROUP BY customer_id, interaction_date` and aggregate functions to capture the latest interaction.
b) **Implement a `WINDOW` function partitioned by `customer_id` and ordered by `interaction_date` to rank interactions and delete lower-ranked duplicates.**
c) Use `DELETE FROM CustomerInteractions WHERE ...` to remove duplicate rows based on a subquery that identifies the earliest interactions.
d) Utilize Delta Lake's time travel feature to revert `CustomerInteractions` to a state before duplicates were introduced.
e) Employ `SELECT DISTINCT customer_id, interaction_date, ... FROM CustomerInteractions` to create a distinct list of interactions.

***Overall explanation***
This question assesses knowledge of handling silver-level data in a Databricks Lakehouse, specifically focusing on deduplication strategies suitable for data at this stage of processing. The correct answer involves using a window function to rank records and then remove duplicates, which aligns with typical data cleaning and refining practices for silver-level datasets.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/functions/window)

```
Domain
SQL in the Lakehouse
```

<br />

#### Q42) In Databricks SQL, when creating a basic, schema-specific visualization, what is the first step you should take?

a) Configure the dashboard settings to match the schema requirements.
b) Adjust the data refresh rate to ensure real-time visualization.
c) Import external visualization libraries for advanced charting.
d) Select the visualization type from the visualization menu.
e) **Write a SQL query to retrieve data from the specific schema.**

***Overall explanation***
When creating basic, schema-specific visualizations using Databricks SQL, the first step is to write a SQL query to retrieve the data you want to visualize. This query will specify the schema you are working with and select the relevant data for visualization. Once you have retrieved the data, you can then proceed to choose the visualization type and configure the visualization settings based on your schema-specific requirements.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/get-started/visualize-data-tutorial)

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q43) In a Databricks dashboard designed to track regional sales data, an analyst introduces a parameter that allows users to select a specific region from a dropdown list. Upon selection, the dashboard updates all visualizations, such as bar charts and line graphs, to reflect sales data exclusively for the chosen region. This change in data display triggered by the parameter selection is an example of which behavior in a Databricks dashboard?

a) The parameter serves as an input field for users to add new data into the dashboard for the selected region.
b) The parameter automatically recalculates the entire dataset for the new region, affecting the data source.
c) The parameter functions as a decorative element, enhancing the visual appeal but not the data content.
d) **The parameter behaves as a dynamic filter, altering the scope of the data presented based on user selection.**
e) The parameter solely adjusts the layout of the dashboard without changing the data displayed.

***Overall explanation***
In Databricks dashboards, parameters like the one described can be used to dynamically filter data. When a user selects a region from the dropdown list, the parameter acts as an interactive control element, filtering and displaying data pertinent only to the chosen region. This behavior allows for a more focused and user-specific data analysis experience, enabling users to interact with the dashboard to extract insights relevant to their specific area of interest. The data itself remains unchanged; the parameter simply controls which subset of it is displayed based on the user's selection.
This feature enhances the interactivity and flexibility of dashboards in Databricks, making them more useful for users who need to analyze different segments of data without manually adjusting data sources or creating multiple dashboards for each subset of data.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/user/queries/query-parameters)

```
Domain
Data Visualization and Dashboards
```

<br />

#### Q44) When conducting a sophisticated analysis in data analytics, which activity best illustrates the thoughtful integration of varied datasets?

a) Focusing exclusively on data from the system with the largest volume of data.
Randomly alternating between systems for data retrieval to maintain diversity.
b) **Synchronizing and unifying data from diverse systems to ensure consistency in analytics.**
c) Separating datasets to preserve the uniqueness of each system's data.
d) Deploying the latest machine learning algorithms without considering data sources.

***Overall explanation***
This question addresses the concept of blending data from two different source applications. It emphasizes the importance of synchronizing and unifying data from various systems, which is a crucial step to ensure that combined data provides consistent and reliable insights in analytics.

```
Domain
Analytics applications
```

<br />

#### Q45) Given the following two tables:

**Customers Table**:

| CustomerId | Name    |
|------------|---------|
| 1          | Alice   |
| 2          | Bob     |
| 3          | Charlie |

**Orders Table**:

| OrderId | CustomerId | OrderAmount |
|---------|------------|-------------|
| 101     | 1          | 300         |
| 102     | 2          | 150         |
| 103     | 4          | 200         |


#### After executing an SQL JOIN query, you obtain the following result:

| CustomerId | Name    | OrderId | OrderAmount |
|------------|---------|---------|-------------|
| 1          | Alice   | 101     | 300         |
| 2          | Bob     | 102     | 150         |
| 3          | Charlie | NULL    | NULL        |

#### What type of JOIN was used in the query?

a) ANTI JOIN
b) CROSS JOIN
c) FULL JOIN
d) INNER JOIN
e) **LEFT JOIN**

***Overall explanation***
This question assesses the understanding of different JOIN types in SQL, specifically focusing on interpreting the output of a JOIN operation. The provided output suggests that all customers are listed, including those without matching orders, indicative of a LEFT JOIN.

References: [Link](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-qry-select-join)

```
Domain
SQL in the Lakehouse
```

<br />