**Question 1Skipped**

What is the purpose of using schema\_of\_json in SQL?

**To validate the structure of a JSON string.**

**To create a new JSON string from a struct.**

**Correct answer**

**To generate a schema that can be used to parse a JSON string into a struct.**

**To compress the JSON data for storage efficiency.**

Overall explanation

schema\_of\_json is used to generate a schema based on an example JSON string, which defines the structure that FROM\_JSON will use to parse JSON strings into structs with the appropriate data types.

**Domain**

ELT With Spark SQL and Python

**Question 2Skipped**

How do you create a permanent view in Databricks SQL that references data from a CSV file stored in DBFS?

**Correct answer**

**Permanent views cannot reference CSV files directly; they must be loaded into a table first.**

**Explanation**

**This is correct:** In Databricks SQL, permanent views cannot directly reference CSV files. Instead, the recommended approach is to load the data from the CSV file into a table first, and then create a permanent view based on that table. This ensures proper data handling and querying capabilities in Databricks SQL.

**Use the CREATE PERMANENT VIEW statement and specify the CSV file path directly after the FROM keyword.**

**Explanation**

**This is incorrect:** The CREATE PERMANENT VIEW statement does not exist in Databricks SQL. Therefore, specifying the CSV file path directly after the FROM keyword is not a valid way to create a permanent view that references data from a CSV file.

**Use the CREATE VIEW statement followed by a SELECT query that uses the csv prefix to reference the CSV file.**

**Explanation**

**This is incorrect:** While it is possible to reference a CSV file in a SELECT query using the csv prefix, this method is not used for creating a permanent view in Databricks SQL. This option does not provide the correct approach for creating a permanent view that references data from a CSV file.

**Use the CREATE VIEW statement with the USING csv option followed by the path to the CSV file.**

**Explanation**

**This is incorrect:** Using the CREATE VIEW statement with the USING csv option is not the correct syntax for creating a permanent view that references data from a CSV file in Databricks SQL. This option is not valid in this context.

Overall explanation

In Databricks, **permanent views** are designed to reference structured tables, not files like CSVs directly stored in DBFS. While it is possible to create temporary views from CSV files for quick access, permanent views demand that data be **transformed into a table format** first. This practice ensures the view can handle updates, schema changes, and other long-term requirements, aligning with Databricks' best practices for data management and SQL operations.

**Domain**

ELT With Spark SQL and Python

**Question 3Skipped**

A Delta Live Table pipeline includes two datasets defined using STREAMING LIVE TABLE. Three datasets are defined against Delta Lake table sources using LIVE TABLE. The table is configured to run in Development mode using the Triggered Pipeline Mode. Assuming previously unprocessed data exists and all definitions are valid, what is the expected outcome after clicking Start to update the pipeline?

**All datasets will be updated once and the pipeline will shut down. The compute resources will be terminated.**

**Explanation**

This choice is incorrect because in Development mode with the Triggered Pipeline Mode, the pipeline will not shut down after updating the datasets once. The compute resources will persist for additional testing and validation.

**Correct answer**

**All datasets will be updated once and the pipeline will shut down. The compute resources will persist to allow for additional testing.**

**Explanation**

This choice is correct because in Development mode with the Triggered Pipeline Mode, the pipeline will update all datasets once and then shut down. However, the compute resources will persist to allow for additional testing and validation.

**All datasets will be updated continuously and the pipeline will not shut down. The compute resources will persist with the pipeline.**

**Explanation**

This choice is incorrect because in Development mode with the Triggered Pipeline Mode, the pipeline will not continuously update datasets. The compute resources will persist with the pipeline after it is stopped for further testing and validation.

**All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will be deployed for the update and terminated when the pipeline is stopped.**

**Explanation**

This choice is incorrect because in Development mode with the Triggered Pipeline Mode, the pipeline will not update datasets at set intervals. The compute resources will be deployed for the update and will persist after the pipeline is stopped for further testing.

**All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will persist after the pipeline is stopped to allow for additional testing.**

**Explanation**

This choice is incorrect because in Development mode with the Triggered Pipeline Mode, the pipeline will not update datasets at set intervals. The compute resources will persist after the pipeline is stopped for additional testing and validation.

Overall explanation

In Development mode using the Triggered Pipeline Mode, a Delta Live Table pipeline is designed to process the available data once per trigger rather than continuously. After clicking Start, the pipeline processes any previously unprocessed data based on the current definitions. Once this batch of data is processed, the pipeline does not actively seek new data until it is triggered again, effectively making it shut down from an active processing perspective. However, in Development mode, compute resources are often maintained even after the pipeline's processing has completed to allow developers to test, debug, and further develop the pipeline without the need for re-provisioning resources. This setup facilitates a more efficient development workflow by reducing the startup time for subsequent test runs.

**Domain**

Incremental Data Processing

**Question 4Skipped**

A data engineer notices that one of the notebooks within a Job, which runs two notebooks as separate tasks, is performing slowly during the Job's current execution. They seek assistance from a tech lead to uncover the reason behind this slow performance. Which approach can the tech lead take to determine why the notebook is running slowly as part of the Job?

**They can navigate to the Runs tab in the Jobs UI to immediately review the processing notebook.**

**Explanation**

Navigating to the Runs tab in the Jobs UI allows the tech lead to review the overall progress and status of the Job, but it may not provide detailed insights into why a specific notebook is running slowly within the Job.

**Correct answer**

**They can navigate to the Runs tab in the Jobs UI and click on the active run to review the processing notebook.**

**Explanation**

By navigating to the Runs tab in the Jobs UI and clicking on the active run, the tech lead can access detailed information about the execution of the notebook within the Job, including performance metrics and potential bottlenecks that may be causing the slow performance.

**They can navigate to the Tasks tab in the Jobs UI to immediately review the processing notebook.**

**Explanation**

Navigating to the Tasks tab in the Jobs UI provides information about the individual tasks within the Job, but it may not directly lead to identifying the specific reasons for the slow performance of the notebook. The Runs tab may offer more comprehensive insights into the overall execution of the Job.

**There is no way to determine why a Job task is running slowly.**

**Explanation**

It is possible to determine why a Job task is running slowly by analyzing the execution details and performance metrics available in the Databricks platform. Therefore, stating that there is no way to determine the reason for slow performance is incorrect.

**They can navigate to the Tasks tab in the Jobs UI and click on the active run to review the processing notebook.**

**Explanation**

Navigating to the Tasks tab in the Jobs UI and clicking on the active run provides information about the individual tasks within the Job, but it may not directly lead to identifying the specific reasons for the slow performance of the notebook.

Overall explanation

The Runs tab in the Jobs UI is specifically designed to provide detailed insights into each execution of a Job, including the performance of individual tasks or notebooks. By selecting the active run associated with the Job in question, the tech lead can access execution logs, runtime metrics, and other pertinent information that can help diagnose performance issues. This direct approach allows for a thorough investigation into the slow-running notebook, offering visibility into potential bottlenecks or areas for optimization within the notebook's execution as part of the Job.

**Domain**

Production Pipelines

**Question 5Skipped**

When integrating the Unity Catalog with existing systems, what is a key advantage?

**It necessitates the use of Databricks-exclusive storage systems.**

**Correct answer**

**It works in concert with existing data, storage, and catalogs, without the need for hard migration.**

**It operates solely on non-standard SQL variants for legacy systems.**

**It requires a complete overhaul of existing data catalogs.**

Overall explanation

The Unity Catalog is designed to integrate seamlessly with existing infrastructure. This means it can work alongside current data solutions, storage systems, and catalogs without requiring a complex and resource-intensive migration process. This integration capability facilitates adoption and minimizes disruption to existing workflows.

**Domain**

Data Governance

**Question 6Skipped**

Which benefit is offered by utilizing array functions in Spark SQL?

**The capability to interact with a collection of tables for streamlined automation.**

**The capability to manipulate data across specific partitions and window frames.**

**The capability to manage time-based data over defined periods.**

**Correct answer**

**The capability to process complex, hierarchical data structures sourced from JSON documents.**

**The capability to handle multiple data types simultaneously.**

Overall explanation

Array functions in Spark SQL are specifically designed to enhance the handling and analysis of complex data types, such as those found in nested arrays or structures typical of JSON files. These functions allow for the manipulation of elements within an array, enabling data engineers and scientists to efficiently process and analyze data that is structured in a hierarchical manner. This is particularly useful when dealing with JSON data, as it often contains nested arrays and objects. By using array functions, users can extract elements, transform array contents, and perform aggregations directly on complex data without needing to flatten the data structure into a relational format first, thereby preserving the original data context and simplifying the analysis of multi-dimensional data.

**Domain**

ELT With Spark SQL and Python

**Question 7Skipped**

Which of the following best describes the difference between Gold tables and Silver tables?

**Gold tables are more likely to contain a less refined view of data than Silver tables.**

**Gold tables are more likely to contain truthful data than Silver tables.**

**Gold tables are more likely to contain valuable data than Silver tables.**

**Gold tables are more likely to contain more data than Silver tables.**

**Correct answer**

**Gold tables are more likely to contain aggregations than Silver tables.**

Overall explanation

Gold tables typically represent a more refined and aggregated view of data compared to Silver tables. In a data lakehouse architecture, Silver tables often consist of cleaned and processed data, but it's the Gold tables that are further refined to include business-level aggregations, metrics, and insights. These tables are designed to support decision-making processes with data that's been summarized from more detailed records found in Silver tables. This aggregation process makes Gold tables particularly valuable for analytics and reporting purposes, providing a higher-level view of the data tailored to specific business needs.

**Domain**

Databricks Lakehouse Platform

**Question 8Skipped**

What syntax does Spark SQL use to query subfields within STRUCT data types?

**Semicolon (;)**

**Colon (:)**

**Correct answer**

**Dot (.)**

**Comma (,)**

Overall explanation

The dot (.) syntax is utilized in Spark SQL to access subfields of STRUCT types. STRUCT types are complex data types in Spark SQL that define a tree of nested fields, and the dot syntax allows for navigating this structure.

**Domain**

ELT With Spark SQL and Python

**Question 9Skipped**

How can Databricks notebooks be shared with external users not part of your Databricks workspace?

**Notebooks cannot be shared externally; they are only accessible within the Databricks workspace.**

**Create a public workspace and move the notebook there for external access.**

**Correct answer**

**Export the notebook as an HTML or IPYNB file and share it via email or file sharing services.**

**Directly share the notebook URL with external users.**

Overall explanation

For sharing notebooks with users outside of the Databricks workspace, exporting the notebook in a universally readable format like HTML or IPYNB is the most effective method. This allows external users to view the notebook content without needing access to the Databricks environment. It ensures compatibility across different platforms and maintains the notebook's formatting and outputs.

**Domain**

Databricks Lakehouse Platform

**Question 10Skipped**

A data engineer is working on a project that involves analyzing a large dataset using Spark SQL. To facilitate the analysis, they create a temporary view of the dataset for preliminary exploration. After creating the temporary view in their current Spark session, they attempt to access this view from a different Spark session for further analysis. Why is the data engineer unable to access the temporary view from a different Spark session?

**Temporary views are automatically deleted after a certain period, and the time limit was exceeded.**

**Temporary views require special permissions to be accessed from different sessions.**

**Correct answer**

**Temporary views in Spark SQL are session-scoped and cannot be accessed from different sessions.**

**The network connection between the two Spark sessions was lost, preventing access to the temporary view.**

**The data engineer did not correctly create the temporary view, resulting in an error when trying to access it from another session.**

Overall explanation

Temporary views in Spark SQL are designed to be session-scoped, meaning they only exist for the duration of the session in which they were created. Once the session ends or if an attempt is made to access the temporary view from a different session, the view will not be available. This behavior is by design, as temporary views are intended for transient, session-specific tasks such as exploratory data analysis or temporary data manipulation. If a view needs to be accessed across multiple sessions, a global temporary view should be used instead. This limitation ensures that temporary views do not consume resources beyond their intended scope and do not interfere with other sessions' operations.

**Domain**

ELT With Spark SQL and Python

**Question 11Skipped**

Which benefit is offered by Delta Lake within the Databricks Lakehouse Platform?

**The capability for simultaneous collaboration within a single notebook**

**Correct answer**

**The capability to handle both batch and streaming data processing tasks**

**The capability to execute complex data operations at scale**

**The capability to work with the same dataset across multiple programming languages**

**The capability to configure notifications for when queries do not execute successfully**

Overall explanation

Delta Lake, an integral component of the Databricks Lakehouse Platform, is designed to bring reliability, performance, and lifecycle management to data lakes. One of its standout features is the ability to support both batch and streaming workloads seamlessly. This means that Delta Lake enables data engineers and scientists to build robust data pipelines that can process data as it arrives in real-time (streaming) and also handle large volumes of data that are not time-sensitive (batch). This dual capability ensures that the Lakehouse can serve a wide range of data processing needs, making it highly versatile for various use cases.

**Domain**

Databricks Lakehouse Platform

**Question 12Skipped**

A data engineer is looking to clean up old or obsolete files from a Delta table's storage to free up space and maintain the efficiency of the table. They aim to remove files that are no longer accessible by any version of the table due to data retention policies. Which keyword should be used to perform this cleanup operation on the Delta table?

**OPTIMIZE**

**Explanation**

The OPTIMIZE keyword is used to optimize the layout of the files in a Delta table to improve query performance. It does not specifically target the removal of old or obsolete files from the table's storage.

**CLEAN**

**Explanation**

The CLEAN keyword is not used for cleaning up old or obsolete files from a Delta table's storage. It is used for cleaning up the metadata of a Delta table by removing any files that are no longer needed for the table's current state.

**REPARTITION**

**Explanation**

The REPARTITION keyword is used to change the partitioning scheme of a Delta table by redistributing the data across partitions. It is not used for cleaning up old or obsolete files from the table's storage.

**Correct answer**

**VACUUM**

**Explanation**

The VACUUM keyword is used to clean up old or obsolete files from a Delta table's storage that are no longer accessible by any version of the table due to data retention policies. It helps free up space and maintain the efficiency of the table by removing these unnecessary files.

**PURGE**

**Explanation**

The PURGE keyword is used to permanently remove files that are no longer needed by a Delta table. However, it does not specifically target files that are no longer accessible by any version of the table due to data retention policies.

Overall explanation

The VACUUM command in Delta Lake is designed to remove old data files that are no longer needed by the table for any active queries or operations. By executing VACUUM, the data engineer can delete files that have surpassed the retention period, effectively cleaning up the storage associated with the Delta table. This operation helps in managing storage costs and improves performance by reducing the amount of unnecessary data that the system needs to manage. Unlike OPTIMIZE, which reorganizes data files for performance, VACUUM specifically targets the removal of obsolete files, making it the correct choice for this scenario.

**Domain**

Incremental Data Processing

**Question 13Skipped**

A data engineer is looking to set a daily refresh schedule for their Databricks SQL dashboard and wishes to have the associated SQL endpoint active only when needed. Which method should the data engineer employ to keep the SQL endpoint's operational time to a minimum for the dashboard's refresh schedule?

**Correct answer**

**They can turn on the Auto Stop feature for the SQL endpoint.**

**They can ensure the dashboard’s SQL endpoint is not one of the included query’s SQL endpoint.**

**They can reduce the cluster size of the SQL endpoint.**

**They can ensure the dashboard’s SQL endpoint matches each of the queries’ SQL endpoints.**

**They can set up the dashboard’s SQL endpoint to be serverless.**

Overall explanation

The Auto Stop feature is designed to automatically stop the SQL endpoint after a period of inactivity. This capability is particularly useful for optimizing resource usage and managing costs, as it ensures that the SQL endpoint is only running when it is actively being used. By enabling Auto Stop, the data engineer can ensure that the SQL endpoint for the dashboard's refresh schedule is automatically shut down when not in use, effectively minimizing its total running time. This approach aligns with best practices for managing Databricks SQL endpoints, allowing for efficient use of resources without manual intervention.

**Domain**

Databricks Lakehouse Platform

**Question 14Skipped**

In Spark SQL, which of the following is necessary to perform a PIVOT operation?

**An aggregate function to summarize the data**

**Explanation**

An aggregate function is used to perform calculations on a set of values and return a single value. While aggregate functions are often used in conjunction with PIVOT operations to summarize data, they are not a necessary component for executing a PIVOT operation in Spark SQL.

**A JOIN clause to merge data from different tables**

**Explanation**

A JOIN clause is used to combine data from multiple tables based on a related column between them. While JOINs are commonly used in SQL queries, they are not specifically necessary for performing a PIVOT operation in Spark SQL.

**A LIMIT clause to restrict the number of rows returned**

**Explanation**

A LIMIT clause is used to restrict the number of rows returned by a query. While limiting the number of rows can be helpful in certain scenarios, such as for performance optimization or data sampling, it is not a requirement for executing a PIVOT operation in Spark SQL.

**Correct answer**

**A DISTINCT clause to ensure unique values**

**Explanation**

A DISTINCT clause is used to remove duplicate rows from the result set of a query. In the context of performing a PIVOT operation in Spark SQL, ensuring unique values using a DISTINCT clause can help in organizing the data correctly before pivoting it.

Overall explanation

When using the PIVOT clause in Spark SQL, it is essential to specify an aggregate function. This function is used to summarize the data that will be transformed from rows into columns. The choice of aggregate function (e.g., SUM, AVG, MAX) depends on the type of summary or analysis required by the user.

**Domain**

ELT With Spark SQL and Python

**Question 15Skipped**

How does setting up an alert for a failed task in Databricks benefit you?

**It automatically fixes any issues with the task.**

**It prevents tasks from failing in the future.**

**It increases the execution speed of future tasks.**

**Correct answer**

**It keeps you informed so you can take immediate action.**

Overall explanation

Setting up an alert for a failed task is like having a smoke alarm in your house. It doesn't stop a fire from happening, but it alerts you right away so you can take quick action. In Databricks, this means you can address issues promptly, minimizing downtime and keeping your data processing on track.

**Domain**

Production Pipelines

**Question 16Skipped**

You are tasked with managing the cloud costs for your data engineering team. After reviewing the monthly expenses, you notice that a significant portion of the budget is being consumed by idle compute resources. You consider implementing Cluster Pools to better manage these costs. How can Cluster Pools help in managing cloud costs effectively in this scenario?

**Correct answer**

**By allowing the specification of a minimum and maximum number of idle instances to prevent over-provisioning.**

**Explanation**

Cluster Pools can effectively manage cloud costs by allowing the specification of a minimum and maximum number of idle instances. This helps prevent over-provisioning of compute resources and ensures efficient utilization, ultimately reducing unnecessary expenses.

**By automatically deleting idle clusters after a specified period.**

**Explanation**

While Cluster Pools can help in managing idle compute resources, they do not automatically delete idle clusters after a specified period. This functionality is not directly related to the purpose of Cluster Pools in cost management.

**By providing unlimited compute resources at a fixed cost.**

**Explanation**

Cluster Pools do not provide unlimited compute resources at a fixed cost. Instead, they help in managing costs by efficiently utilizing existing resources and preventing over-provisioning.

**By encrypting data at rest and in transit, reducing compliance costs.**

**Explanation**

Encrypting data at rest and in transit is important for security and compliance but is not directly related to the cost management aspect of using Cluster Pools. This functionality focuses on data security rather than cost optimization.

Overall explanation

Cluster Pools can help manage cloud costs effectively by allowing administrators to specify a minimum and maximum number of idle instances. This feature prevents over-provisioning of resources, ensuring that you only pay for what you need, while still keeping resources readily available for new or scaling clusters.

**Domain**

Incremental Data Processing

**Question 17Skipped**

What functionality does Data Access Audit provide in a data governance framework?

**It restricts any access to the data.**

**It hides data access details from administrators.**

**It allows users to modify data without tracking.**

**Correct answer**

**It provides a mechanism to capture and record all access to data.**

Overall explanation

Data Access Audit is essential for accountability and compliance in data governance. It involves tracking and logging data access, which is crucial for auditing and understanding how data is being used, by whom, and for what purpose. In Databricks, auditing can help in monitoring data access patterns and ensuring compliance with data governance policies.

**Domain**

Data Governance

**Question 18Skipped**

What tool does Auto Loader utilize to process data in increments?

**Data Explorer**

**Explanation**

Data Explorer is a tool in Databricks that allows users to explore and analyze data interactively. While it is useful for data exploration and visualization, it is not the tool utilized by Auto Loader for processing data in increments.

**Unity Catalog**

**Explanation**

Unity Catalog is a centralized metadata management service in Databricks that helps users discover, organize, and query data. While it is important for managing metadata, it is not the tool used by Auto Loader for processing data in increments.

**Correct answer**

**Spark Structured Streaming**

**Explanation**

Spark Structured Streaming is the correct choice for the tool utilized by Auto Loader to process data in increments. Spark Structured Streaming allows for real-time processing of data streams and supports processing data in micro-batches, making it ideal for incremental data processing.

**Checkpointing**

**Explanation**

Checkpointing is a mechanism used in Spark to ensure fault tolerance and recoverability in case of failures. While it is important for maintaining the state of a streaming application, it is not directly related to the processing of data in increments by Auto Loader.

**Databricks SQL**

**Explanation**

Databricks SQL is a SQL analytics service in Databricks that allows users to query and analyze data using SQL. While it is a useful tool for querying data, it is not the tool utilized by Auto Loader for processing data in increments.

Overall explanation

Auto Loader is designed to efficiently ingest data incrementally into Delta Lake on Databricks, leveraging Spark Structured Streaming for this purpose. Spark Structured Streaming provides a high-level abstraction for stream processing, allowing for scalable and fault-tolerant processing of streaming data. It enables Auto Loader to monitor a directory for new files and process them as they arrive, handling incremental loads with ease. This approach simplifies the development of streaming data pipelines, ensuring that data is processed in real-time or near-real-time, making Spark Structured Streaming the ideal tool within Auto Loader's architecture for incremental data processing.

**Domain**

Incremental Data Processing

**Question 19Skipped**

A data analyst needs to enhance their Databricks notebook by adding complex data visualizations created with R, alongside existing Python analytics code. What is the best approach?

**Convert R visualization code to Python before integrating.**

**Export the data from Python to an R script outside Databricks.**

**Correct answer**

**Use the %r magic command in new cells specifically for R-based visualizations.**

**Rewrite the Python analytics code in R for consistency.**

Overall explanation

By using the %r magic command, the analyst can seamlessly integrate R for data visualization without altering the existing Python analytics code. This approach utilizes Databricks' support for multiple languages to enhance data analysis and visualization within a single notebook.

**Domain**

Databricks Lakehouse Platform

**Question 20Skipped**

A data engineer needs to run the last part of a Python script only if the variable day\_of\_week is 1 and review\_period is True. Which line of code should they use to start this part of the script?

**if day\_of\_week = 1 and review\_period**

**if day\_of\_week == 1 and review\_period == "True"**

**Correct answer**

**if day\_of\_week == 1 and review\_period**

**if day\_of\_week = 1 & review\_period: = "True"**

**if day\_of\_week = 1 and review\_period = "True"**

Overall explanation

The correct line of code checks if day\_of\_week equals 1 using double equals == and also checks if review\_period is true without comparing it to the string "True". Why?: In Python, to compare values, you should use ==, not =, because = is used for assigning values. The correct option does this by using == to compare day\_of\_week to 1. It also simply checks if review\_period is true, which is the right way to check a boolean (true or false) value in Python. Some incorrect options try to compare a boolean to a string or use assignment operators in a condition, which is not how you write conditions in Python. The best choice directly and correctly checks both conditions without unnecessary comparisons or syntax errors.

**Domain**

ELT With Spark SQL and Python

**Question 21Skipped**

Given a table named 'events' with a column 'items' that contains an array of values, which SQL query would transform each item of the array into a separate row?

**SELECT user\_id, event\_timestamp, event\_name, ARRAY(items) AS item FROM events;**

**SELECT user\_id, event\_timestamp, event\_name, UNNEST(items) AS item FROM events;**

**SELECT user\_id, event\_timestamp, event\_name, items FROM events;**

**Correct answer**

**SELECT user\_id, event\_timestamp, event\_name, explode(items) AS item FROM events;**

Overall explanation

SELECT user\_id, event\_timestamp, event\_name, explode(items) AS item FROM events;

**Domain**

ELT With Spark SQL and Python

**Question 22Skipped**

If you are analyzing data in a Databricks notebook using Python, how can you switch to SQL for a specific visualization task?

**Convert the entire notebook to SQL mode from the notebook settings.**

**Export the data to a SQL database and create a new SQL notebook.**

**Correct answer**

**Use the %sql magic command in a new cell where you want to start using SQL.**

**Rewrite the Python code in SQL and run it in a new Python cell.**

Overall explanation

The %sql magic command allows users to switch the execution context to SQL for specific cells within a notebook. This feature enables data scientists and engineers to leverage the strengths of SQL for data querying and visualization directly within a multi-language notebook.

**Domain**

Databricks Lakehouse Platform

**Question 23Skipped**

When querying data from a JSON file in Databricks SQL, what prefix should be included after the FROM keyword?

**Correct answer**

**json**

**JSON**

**FILE**

**TEXT**

Overall explanation

To query data directly from JSON files using Databricks SQL, the prefix json is used after the FROM keyword, followed by the path to the JSON file. This indicates that the query should interpret the source data as JSON format, enabling SQL-like querying capabilities on semi-structured JSON data.

**Domain**

ELT With Spark SQL and Python

**Question 24Skipped**

How would you define a column in a SQL table that includes multiple attributes like street, city, and zip code?

**As separate VARCHAR columns**

**As a MAP with key-value pairs**

**As a single TEXT column with concatenated values**

**Correct answer**

**As a STRUCT with nested attributes**

Overall explanation

A STRUCT data type allows for the creation of an ordered collection of columns with potentially different data types. This makes it suitable for defining complex data such as an address, which consists of multiple attributes like street, city, and zip code.

**Domain**

ELT With Spark SQL and Python

**Question 25Skipped**

You are designing a data processing workflow in Databricks and have ingested raw IoT device data into bronze tables. Your next step is to clean and normalize this data. Which table should you store the cleaned and normalized data in?

**A gold table**

**Correct answer**

**A silver table**

**Directly into an external data warehouse**

**Another bronze table**

Overall explanation

After ingesting raw data into bronze tables, the next step in a typical Databricks data processing workflow involves cleaning and normalizing the data, which should then be stored in silver tables. Silver tables are used for intermediate processed data that has been cleaned and is ready for further analysis or transformation.

**Domain**

Databricks Lakehouse Platform

**Question 26Skipped**

What kind of tasks can be monitored in the Databricks job run interface?

**Correct answer**

**Any tasks that are part of a job workflow**

**Only data ingestion tasks**

**Only tasks related to database management**

**Only machine learning training tasks**

Overall explanation

The job run interface in Databricks is designed to monitor all types of tasks that are part of a job workflow, including but not limited to data ingestion, processing, analysis, and machine learning training. This ensures that users have visibility into the entire workflow execution.

**Domain**

Production Pipelines

**Question 27Skipped**

In the context of Databricks workflows, which pattern would you use to handle data at different stages of refinement, from raw to ready-for-use?

**Fan-out**

**Funnel**

**Correct answer**

**Sequence**

**Loop**

Overall explanation

In Databricks, a sequence pattern aligns with the concept of table stages in a Delta Lake architecture, which is often structured around bronze (raw data), silver (cleaned or conformed data), and gold (aggregated or report-ready data) tables. Each stage represents a step in the data transformation process, with dependencies that ensure data moves through the pipeline in a controlled and sequential manner.

**Domain**

Production Pipelines

**Question 28Skipped**

How do you create a permanent view in Databricks SQL that references data from a CSV file stored in DBFS?

**Use the CREATE VIEW statement with the USING csv option followed by the path to the CSV file.**

**Permanent views cannot reference CSV files directly; they must be loaded into a table first.**

**Correct answer**

**Use the CREATE VIEW statement followed by a SELECT query that uses the csv prefix to reference the CSV file.**

**Use the CREATE PERMANENT VIEW statement and specify the CSV file path directly after the FROM keyword.**

Overall explanation

In Databricks SQL, you can create a permanent view by using the CREATE VIEW statement followed by a SELECT query. To reference a CSV file directly, you use the csv prefix after the FROM keyword in your SELECT query within the view definition. This allows you to create a view that directly references and queries data from a CSV file stored in DBFS.

**Domain**

ELT With Spark SQL and Python

**Question 29Skipped**

A data engineer is preparing to utilize a Delta table for a data pipeline project but is unsure about their access permissions for the table. Where can the data engineer check to confirm their permissions on the Delta table?

**Databricks Filesystem**

**Dashboards**

**Repos**

**Correct answer**

**Data Explorer**

**Jobs**

Overall explanation

The Data Explorer in Databricks is specifically designed for users to explore, manage, and understand data stored within the platform, including Delta tables. It not only allows users to browse and query data but also to view detailed metadata and permissions associated with the tables. By using Data Explorer, the data engineer can easily verify their permissions on a Delta table, ensuring they have the necessary access to use it as part of their data pipeline. This capability is crucial for managing data access and security within Databricks, facilitating smooth and secure data pipeline operations.

**Domain**

Data Governance

**Question 30Skipped**

Which of the following Structured Streaming queries is performing a hop from a Silver table to a Gold table?

1.  (spark.table("sales")
2.  .withColumn("avgPrice", col("sales") / col("units"))
3.  .writeStream
4.  .option("checkpointLocation", checkpointPath)
5.  .outputMode("append")
6.  .table("newSales")
7.  )

**Explanation**

This query is taking the existing "sales" table, adding a new column "avgPrice", and writing it to a newSales table. It does not involve a hop from a Silver table to a Gold table as it is not transforming the data significantly.

1.  (spark.table("sales")
2.  .filter(col("units") > 0)
3.  .writeStream
4.  .option("checkpointLocation", checkpointPath)
5.  .outputMode("append")
6.  .table("newSales")
7.  )

**Explanation**

This query is filtering the "sales" table based on units and writing it to a newSales table. It does not involve any aggregation or transformation that would classify it as a hop from a Silver table to a Gold table.

1.  (spark.readStream.load(rawSalesLocation)
2.  .writeStream
3.  .option("checkpointLocation", checkpointPath)
4.  .outputMode("append")
5.  .table("newSales")
6.  )

**Explanation**

This query is loading data from a raw sales location and writing it to a newSales table. It does not involve any transformation or aggregation that would classify it as a hop from a Silver table to a Gold table.

1.  (spark.read.load(rawSalesLocation)
2.  .writeStream
3.  .option("checkpointLocation", checkpointPath)
4.  .outputMode("append")
5.  .table("newSales")
6.  )

**Explanation**

This query is loading data from a raw sales location and writing it to a newSales table. It does not involve any transformation or aggregation that would classify it as a hop from a Silver table to a Gold table.

**Correct answer**

1.  (spark.table("sales")
2.  .groupBy("store")
3.  .agg(sum("sales"))
4.  .writeStream
5.  .option("checkpointLocation", checkpointPath)
6.  .outputMode("complete")
7.  .table("newSales")
8.  )

**Explanation**

This query is taking the "sales" table, grouping by "store", aggregating the sum of sales, and writing it to a newSales table. This transformation from the original "sales" table to the aggregated "newSales" table qualifies as a hop from a Silver table to a Gold table.

Overall explanation

The process of grouping and summing sales data, then writing the full aggregated results to a table, aligns with the characteristics of a transformation from a Silver to a Gold table in a typical data architecture. The justification for the answer that involves a grouping by "store" and an aggregation of the sum of "sales", followed by a write stream to a table with the "complete" output mode, is the one that fits the description of a hop from a Silver table to a Gold table for the following reasons: Aggregation: The inclusion of an aggregation function, specifically summing up the sales, is a classic operation in the process of creating a Gold table. Gold tables typically contain aggregated metrics that are derived from more granular data. Grouping: The data is being grouped by a key, in this case, "store", which indicates a summarization of data at a higher level of abstraction. This is typical for Gold tables where the data is often needed at an aggregated level for analysis or reporting purposes. Output Mode: The "complete" output mode signifies that the entire updated result table is being outputted, which is essential for aggregation queries since it reflects the most current aggregated view. This contrasts with the "append" output mode which would be more typical for an incremental update, not a full aggregation. Contextual Fit: In a multi-tiered storage model where data is refined from raw to aggregated states (often referred to as Bronze, Silver, and Gold), the described transformation aligns with the final stage. It reflects the enrichment and summarization processes consistent with the creation of a Gold table.

**Domain**

Incremental Data Processing

**Question 31Skipped**

A data engineer is implementing a data pipeline that requires updates, deletions, and merges into a large dataset stored on a distributed file system. The engineer needs to ensure that these operations are performed reliably and consistently, with the ability to handle concurrent modifications without data corruption or loss. Which feature should the data engineer leverage to guarantee atomic, consistent, isolated, and durable (ACID) transactions within their data pipeline?

**Correct answer**

**Delta Lake**

**Explanation**

Delta Lake is a powerful feature that provides ACID transactions for big data processing. It ensures atomicity, consistency, isolation, and durability for data operations, making it a suitable choice for the data engineer to guarantee reliable and consistent updates, deletions, and merges within the data pipeline.

**Dataframe caching**

**Explanation**

Dataframe caching is a mechanism used to store intermediate results in memory to improve query performance. While caching can enhance data processing speed, it does not provide the necessary features to ensure ACID transactions and handle concurrent modifications in a distributed file system.

**Data partitioning**

**Explanation**

Data partitioning is a technique used to divide large datasets into smaller, more manageable parts. While data partitioning can improve query performance and scalability, it does not guarantee ACID transactions or provide mechanisms for handling concurrent modifications and ensuring data consistency.

**Spark Streaming**

**Explanation**

Spark Streaming is a real-time processing engine that enables continuous data processing. While Spark Streaming is valuable for processing streaming data, it does not inherently provide the ACID transaction capabilities required for ensuring data consistency and reliability in batch processing scenarios.

**File compaction**

**Explanation**

File compaction is a process of reducing the size of data files by merging them together. While file compaction can optimize storage efficiency, it does not offer the necessary features to guarantee ACID transactions and handle concurrent modifications in a distributed file system for maintaining data integrity and consistency.

Overall explanation

Delta Lake provides ACID transactions on top of Spark's data processing capabilities. This means it can handle complex operations like updates, deletes, and merges in a way that ensures data integrity, even when multiple processes are reading from and writing to the dataset simultaneously. Delta Lake's transaction log records every change made to the data, allowing for atomic and consistent operations, isolation among concurrent transactions, and durability to ensure data is never lost. This makes Delta Lake an ideal choice for data engineers who need to manage large datasets with high reliability and consistency.

**Domain**

Incremental Data Processing

**Question 32Skipped**

Which of the following can be used to simplify and unify siloed data architectures that are specialized for specific use cases?

**None of these**

**All of these**

**Correct answer**

**Data lakehouse**

**Data warehouse** 

**Data lake** 

Overall explanation

A data lakehouse is designed to simplify and unify siloed data architectures by combining the best features of data lakes and data warehouses. It supports the vast data storage capabilities of a data lake, allowing for the storage of structured and unstructured data at scale, while also incorporating the data management and ACID transaction features typically associated with data warehouses. This approach enables organizations to handle diverse data types and use cases within a single, unified architecture, reducing complexity and eliminating the need for separate systems for different data processing tasks.

**Domain**

Databricks Lakehouse Platform

**Question 33Skipped**

Your Databricks job occasionally fails due to transient network issues. You want to ensure that the job automatically retries before sending an alert about the failure. How should you configure the job to address this requirement?

**Correct answer**

**Set up a retry policy with a specific number of retries before an alert is sent.**

**Disable all alerts for the job to avoid unnecessary notifications.**

**Increase the job's resource allocation to prevent failures.**

**Manually restart the job whenever it fails.**

Overall explanation

A retry policy automatically handles transient issues by attempting to rerun the job a certain number of times before considering it a failure. This approach is like giving a student multiple attempts at a test question before marking it wrong, increasing the chances of success without manual intervention.

**Domain**

Production Pipelines

**Question 34Skipped**

For what reason would a data engineer specify a Task in the "Depends On" field while configuring a new Task in a Databricks Job?

**Correct answer**

**When another task needs to successfully complete before the new task begins**

**When another task needs to fail before the new task begins**

**When another task needs to be replaced by the new task**

**When another task needs to use as little compute resources as possible**

**When another task has the same dependency libraries as the new task**

Overall explanation

This approach is essential for managing task dependencies within Databricks Jobs, ensuring that certain tasks are completed successfully before others commence. It helps maintain a logical sequence in workflows, where the outcome of preceding tasks directly influences the initiation and execution of subsequent tasks, thereby optimizing the workflow's efficiency and effectiveness.

**Domain**

Production Pipelines

**Question 35Skipped**

What is a critical step in orchestrating a task within a Databricks Workflow Job?

**Choosing a color scheme for task visualization**

**Correct answer**

**Setting up task dependencies to control execution order**

**Configuring the maximum allowable downtime for the job**

**Assigning a unique identifier to each task manually**

Overall explanation

Task dependencies are essential for task orchestration as they define the order in which tasks should be executed. This allows for tasks to be run based on the successful completion of preceding tasks, ensuring the workflow is logical and efficient.

**Domain**

Production Pipelines

**Question 36Skipped**

What is the impact of terminating a cluster in Databricks?

**Terminating a cluster removes all user access permissions and configurations permanently.**

**Correct answer**

**Terminating a cluster releases the compute resources but retains the data in attached storage, allowing for data persistence.**

**Terminating a cluster automatically archives all its notebooks and configurations for future restoration.**

**Terminating a cluster permanently deletes all data stored in the cluster's associated storage.**

Overall explanation

Upon termination, a cluster's compute resources are released, meaning that the virtual machines or instances that were running are stopped. However, the data stored in Databricks-managed storage or attached storage (like DBFS or mounted storage from cloud providers) is not deleted. This ensures that while compute resources are managed efficiently, data persistence is maintained for future use or analysis. Any temporary data stored in the cluster's local storage is lost.

**Domain**

Databricks Lakehouse Platform

**Question 37Skipped**

Why is Z-ordering beneficial for Delta Lake tables?

**Correct answer**

**It arranges data to reduce the number of files scanned during queries.**

**It encrypts data for enhanced security.**

**It increases the size of the table for better performance.**

**It organizes files into a random order to enhance query speed.**

Overall explanation

Z-ordering is a technique used in Delta Lake tables to optimize the physical layout of data. By co-locating related information in the same set of files, Z-ordering minimizes the amount of data scanned during queries. This is particularly beneficial for queries with filters, as it significantly reduces the number of files that need to be read, thereby improving query performance. Unlike encryption or increasing table size, the primary goal of Z-ordering is to make data retrieval more efficient by organizing data in a way that aligns with common query patterns.

**Domain**

Incremental Data Processing

**Question 38Skipped**

A data engineer is attempting to drop a Spark SQL table my\_table and runs the following command: DROP TABLE IF EXISTS my\_table; After running this command, the engineer notices that the data files and metadata files have been deleted from the file system. Which of the following describes why all of these files were deleted?

**The table was external**

**Explanation**

If the table was external, the data and metadata files would not have been deleted from the file system when dropping the table. External tables do not manage the data files, so dropping the table only removes the table definition.

**The table's data was smaller than 10 GB**

**Explanation**

The size of the table's data does not affect whether the data files and metadata files are deleted when dropping a table in Spark SQL. The deletion of files is determined by whether the table is managed or external.

**The table did not have a location**

**Explanation**

If the table did not have a location specified, it would have been an in-memory table, and dropping it would not delete any data or metadata files from the file system. In this case, the absence of a location would prevent the deletion of files upon dropping the table.

**The table's data was larger than 10 GB**

**Explanation**

The size of the table's data being larger than 10 GB does not impact the deletion of data and metadata files when dropping a table in Spark SQL. The deletion is based on whether the table is managed or external.

**Correct answer**

**The table was managed**

**Explanation**

When a Spark SQL table is managed, the DROP TABLE command not only removes the table definition but also deletes the associated data and metadata files from the file system. This is why all files related to the table were deleted in this scenario.

Overall explanation

In Databricks, when you drop a managed table using the DROP TABLE command, it not only deletes the table metadata but also the associated data files. Managed tables are physically managed by Databricks, and dropping them results in the removal of both metadata and data files, ensuring a clean removal of the table and its data.

**Domain**

Incremental Data Processing

**Question 39Skipped**

What level of access does a Metastore Admin have in the Databricks Unity Catalog?

**They can only view metadata and user management settings.**

**They can manage users and groups as well as integrate with IdP.**

**They have full access to all data objects.**

**Correct answer**

**They can create or drop, grant privileges on, and change the ownership of catalogs and other data objects.**

Overall explanation

The Metastore Admin role is designed to handle specific data catalog management tasks, including the creation, modification, and assignment of privileges on data objects within the metastore, reflecting a focused scope of responsibilities related to data governance and organization.

**Domain**

Data Governance

**Question 40Skipped**

Given the task of dynamically generating a table name in Python using variables for region, store, and year, where the desired format for the table name is akin to "la200\_sales\_2022" for region="la", store="200", and year="2022", which Python command would correctly construct this table name?

**f"{region}+{store}+\_sales\_+{year}"**

**"{region}{store}\_sales\_{year}"**

**Correct answer**

**f"{region}{store}\_sales\_{year}"**

**"{region}+{store}+\_sales\_+{year}"**

Overall explanation

To dynamically create a string in Python that incorporates variables directly into the string's content, the f-string syntax (f"") is used. This method allows for embedding expressions inside string literals efficiently. The correct format, f"{region}{store}\_sales\_{year}", seamlessly integrates the variables region, store, and year with the static string parts to generate the desired table name. This approach is straightforward and eliminates the need for concatenation operators (+), providing a clean and readable solution for string construction with variable content.

**Domain**

ELT With Spark SQL and Python

**Question 41Skipped**

When a data engineer and data analyst decide to transition their data pipeline to Delta Live Tables, considering their use of Python for the raw, bronze, and silver layers, and SQL for the gold layer, with a streaming input as the raw source, which adjustment is necessary?

**The pipeline will need to use a batch source in place of a streaming source**

**The pipeline will need to be written entirely in Python**

**The pipeline will need to stop using the medallion-based multi-hop architecture**

**The pipeline will need to be written entirely in SQL**

**Correct answer**

**None of these changes will need to be made**

Overall explanation

Delta Live Tables (DLT) supports both Python and SQL for defining data transformations, allowing teams to leverage the strengths of both languages within the same pipeline. DLT is designed to handle streaming inputs efficiently, making it fully compatible with pipelines that ingest data in real-time. Furthermore, the medallion architecture (raw, bronze, silver, and gold layers) aligns well with DLT's capabilities for managing and processing data at different stages of refinement. Therefore, migrating to Delta Live Tables does not require abandoning the use of multiple programming languages, switching from a streaming to a batch source, or moving away from the established multi-hop architecture. Delta Live Tables enhances the pipeline with features like automatic error handling, data quality checks, and simplified management, without necessitating fundamental changes to the pipeline's design or operation.

**Domain**

Incremental Data Processing

**Question 42Skipped**

Assuming you have a Delta Lake table named customer\_purchases that contains duplicate records based on customer\_id. You want to deduplicate these records by keeping only the latest purchase record for each customer based on a purchase\_date. How would you use the MERGE statement in Databricks SQL to achieve this?

**Correct answer**

1.  MERGE INTO customer\_purchases AS target
2.  USING (
3.  SELECT customer\_id, MAX(purchase\_date) AS latest\_purchase\_date
4.  FROM customer\_purchases
5.  GROUP BY customer\_id
6.  ) AS source
7.  ON target.customer\_id = source.customer\_id AND target.purchase\_date < source.latest\_purchase\_date
8.  WHEN MATCHED THEN DELETE;
9.  MERGE INTO customer\_purchases AS target
10.  USING customer\_purchases AS source
11.  ON target.customer\_id = source.customer\_id
12.  WHEN MATCHED AND target.purchase\_date < source.purchase\_date THEN DELETE;
13.  MERGE INTO customer\_purchases AS target
14.  USING (
15.  SELECT customer\_id, MAX(purchase\_date) AS latest\_purchase\_date
16.  FROM customer\_purchases
17.  GROUP BY customer\_id
18.  ) AS source
19.  ON target.customer\_id = source.customer\_id
20.  WHEN MATCHED AND target.purchase\_date < source.latest\_purchase\_date THEN DELETE
21.  WHEN NOT MATCHED THEN INSERT \*;
22.  MERGE INTO customer\_purchases AS target
23.  USING (
24.  SELECT customer\_id, purchase\_date
25.  FROM customer\_purchases
26.  WHERE purchase\_date = (SELECT MAX(purchase\_date) FROM customer\_purchases GROUP BY customer\_id)
27.  ) AS source
28.  ON target.customer\_id = source.customer\_id
29.  WHEN MATCHED THEN UPDATE SET target.\* = source.\*
30.  WHEN NOT MATCHED THEN INSERT \*;

Overall explanation

This option correctly identifies the use of MERGE for deduplication in Delta Lake. It merges the customer\_purchases table with a subquery that selects the latest purchase\_date for each customer\_id. The ON clause matches records in the target table with those in the source based on customer\_id and where the target's purchase\_date is less than the source's latest\_purchase\_date, indicating that there are newer records. The WHEN MATCHED THEN DELETE clause ensures that only the most recent purchase record for each customer is retained, effectively deduplicating the table based on the specified criteria.

**Domain**

ELT With Spark SQL and Python

**Question 43Skipped**

Which component resides in the customer's cloud account in the Databricks architecture?

**Correct answer**

**Data storage and computation resources.**

**Cluster management services.**

**The Databricks workspace interface.**

**Databricks' internal metadata store.**

Overall explanation

The data plane, which includes data storage and computation resources, resides within the customer's cloud account.

**Domain**

Databricks Lakehouse Platform

**Question 44Skipped**

You are tasked with managing the cloud costs for your data engineering team. After reviewing the monthly expenses, you notice that a significant portion of the budget is being consumed by idle compute resources. You consider implementing Cluster Pools to better manage these costs. How can Cluster Pools help in managing cloud costs effectively in this scenario?

**By encrypting data at rest and in transit, reducing compliance costs.**

**By automatically deleting idle clusters after a specified period.**

**Correct answer**

**By allowing the specification of a minimum and maximum number of idle instances to prevent over-provisioning.**

**By providing unlimited compute resources at a fixed cost.**

Overall explanation

Cluster Pools can help manage cloud costs effectively by allowing administrators to specify a minimum and maximum number of idle instances. This feature prevents over-provisioning of resources, ensuring that you only pay for what you need, while still keeping resources readily available for new or scaling clusters.

**Domain**

Databricks Lakehouse Platform

**Question 45Skipped**

Which of the following must be specified when creating a new Delta Live Tables pipeline?

**A path to cloud storage location for the written data**

**Explanation**

Specifying a path to cloud storage location for the written data is important for storing the output of the pipeline, but it is not a mandatory requirement for creating a new Delta Live Tables pipeline. The storage location can be configured later in the pipeline settings.

**The preferred DBU/hour cost**

**Explanation**

The preferred DBU/hour cost is not a mandatory requirement when creating a new Delta Live Tables pipeline. The DBU/hour cost is related to Databricks Unit (DBU) usage and cost management, but it is not directly tied to the creation of the pipeline.

**A location of a target database for the written data**

**Explanation**

While specifying a location of a target database for the written data can be beneficial for data organization and management, it is not a mandatory requirement when creating a new Delta Live Tables pipeline. The target database can be defined at a later stage in the pipeline configuration.

**Correct answer**

**At least one notebook library to be executed**

**Explanation**

At least one notebook library to be executed is a mandatory requirement when creating a new Delta Live Tables pipeline. The notebook library contains the code and logic that will be executed as part of the pipeline, making it an essential component for the pipeline creation process.

**A key-value pair configuration**

**Explanation**

A key-value pair configuration is not a requirement when creating a new Delta Live Tables pipeline. While configurations can be specified for various settings, it is not mandatory for the creation of the pipeline.

Overall explanation

Specifying at least one notebook or library with the defined Delta Live Tables syntax is mandatory for the creation and execution of a Delta Live Tables pipeline. This requirement is crucial for defining the transformations and operations that the pipeline will perform on the data.

**Domain**

Incremental Data Processing