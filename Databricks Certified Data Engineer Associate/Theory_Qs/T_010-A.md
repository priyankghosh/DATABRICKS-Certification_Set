**Question 1Skipped**

Which of the following SQL keywords can be used to convert a table from a long format to a wide format?

**SUM**

**CONVERT**

**Correct answer**

**PIVOT**

**TRANSFORM**

**WHERE**

Overall explanation

The PIVOT SQL keyword is specifically designed for transforming data from a long format to a wide format. This operation allows for the rotation of data, turning unique values from one column into multiple columns in the output, effectively summarizing and reorganizing the data. It's particularly useful for creating summary reports and for data analysis purposes, where seeing data in a wide format can provide more insights at a glance than the long format.

**Domain**

ELT With Spark SQL and Python

**Question 2Skipped**

A data engineer working in Databricks wants to create a User-Defined Function (UDF) to convert temperatures from Celsius to Fahrenheit for a dataset containing global weather information. Which of the following best describes the steps they should take to accomplish this within a Databricks notebook?

**Directly modify the dataset in the data lake to convert all temperatures to Fahrenheit before importing it into Databricks.**

**Correct answer**

**Create a UDF in Databricks that applies the conversion formula and use it in a Spark SQL query to transform the temperature column.**

**Use a built-in Spark SQL function that automatically converts temperatures from Celsius to Fahrenheit.**

**Export the dataset to a CSV file, convert the temperatures using an external tool, and then re-import the dataset into Databricks.**

Overall explanation

Creating a UDF in Databricks allows for custom transformations to be applied directly within Spark SQL queries, offering a flexible and efficient way to manipulate data. For the task of converting temperatures from Celsius to Fahrenheit, the UDF would encapsulate the conversion formula (Fahrenheit = Celsius \* 9/5 + 32). This method is more efficient and scalable within Databricks compared to manually modifying data or relying on external tools. Example code snippet for creating and using the UDF in Databricks: # Define the UDF in Python def celsiusToFahrenheit(celsius): return (celsius \* 9.0/5) + 32 # Register the UDF spark.udf.register("celsiusToFahrenheitUDF", celsiusToFahrenheit) # Example usage in Spark SQL spark.sql("SELECT city, celsiusToFahrenheitUDF(temperature) AS temperature\_fahrenheit FROM weather\_data") This approach leverages Databricks' capability to integrate PySpark and Spark SQL for data processing, enabling the data engineer to apply custom data transformations efficiently within their analytical workflows.

**Domain**

ELT With Spark SQL and Python

**Question 3Skipped**

Which command allows for data to be inserted into a Delta table while ensuring duplicates are not written?

**APPEND**

**Correct answer**

**MERGE**

**DROP**

**UPDATE**

**INSERT**

Overall explanation

The MERGE command in Delta Lake is specifically designed to upsert data into a Delta table by either updating existing records or inserting new records, based on a set of match criteria. This capability makes it particularly effective for avoiding duplicate records. By using MERGE, you can specify conditions under which rows should be updated if they match an existing row (thus preventing duplicates) or inserted as new rows if no match is found. This command is essential for maintaining data integrity in scenarios where the dataset may contain updates to existing records or entirely new records, ensuring that the Delta table remains free of duplicates while being updated with the latest information.

**Domain**

Incremental Data Processing

**Question 4Skipped**

Why might you set up a retry policy for Databricks jobs?

**Correct answer**

**To automatically attempt to rerun tasks that fail, improving reliability.**

**To ensure that a job runs continuously without stopping.**

**To increase the job's execution time for detailed analysis.**

**To comply with data processing regulations.**

Overall explanation

A retry policy is like having a backup plan when something doesn't go as expected. If a task fails, Databricks will try again based on the policy you set. This means less manual work for you and a better chance that your data processing will succeed eventually.

**Domain**

Production Pipelines

**Question 5Skipped**

What prefix is used in Databricks SQL to query data directly from Parquet files?

**FILE**

**DATA**

**Correct answer**

**parquet**

**PARQUET**

Overall explanation

Databricks SQL, the prefix parquet is used after the FROM keyword to specify that the data source is a Parquet file. This allows users to perform SQL queries directly on Parquet files by providing the path to the file, leveraging Parquet's efficient columnar storage format for fast and flexible data analysis.

**Domain**

ELT With Spark SQL and Python

**Question 6Skipped**

When creating a table in Databricks using a JDBC connection, which of the following SQL commands correctly attempts to create a new table and throws an error if the table with that name already exists?

1.  INSERT INTO new\_table
2.  SELECT \* FROM jdbc('jdbc:mysql://database.example.com:3306/mydb', 'existing\_table', 'username', 'password')
3.  CREATE TABLE new\_table AS
4.  SELECT \* FROM jdbc('jdbc:mysql://database.example.com:3306/mydb', 'table\_name', 'username', 'password')
5.  CREATE TABLE IF NOT EXISTS new\_table
6.  USING jdbc
7.  OPTIONS (
8.  url 'jdbc:mysql://database.example.com:3306/mydb',
9.  dbtable 'existing\_table',
10.  user 'username',
11.  password 'password'
12.  )

**Correct answer**

1.  CREATE TABLE new\_table
2.  USING jdbc
3.  OPTIONS (
4.  url 'jdbc:mysql://database.example.com:3306/mydb',
5.  dbtable 'new\_table',
6.  user 'username',
7.  password 'password'
8.  )

Overall explanation

This SQL command for creating a new table through a JDBC connection in Databricks specifies the JDBC connection details and attempts to create a new table named new\_table. If a table with that name already exists, Databricks will throw an error, which aligns with the default behavior for creating tables via JDBC connections.

**Domain**

ELT With Spark SQL and Python

**Question 7Skipped**

What is a benefit of using multiple tasks in Databricks Jobs?

**Reduces the overall execution time**

**Limits the scope of data processing**

**Correct answer**

**Allows for sequential task execution**

**Increases the complexity of jobs**

Overall explanation

Multiple tasks in Jobs allow for the organization of complex workflows where tasks can be executed in a specific sequence, often depending on the completion of predecessor tasks. This enables more efficient data processing pipelines.

**Domain**

Production Pipelines

**Question 8Skipped**

How does the VACUUM command manage deletes in Delta Lake tables?

**Correct answer**

**By cleaning up older, unnecessary files after a retention period.**

**By immediately removing all deleted records from the table.**

**By marking deleted records as inactive for immediate purging.**

**By encrypting deleted records for security purposes.**

Overall explanation

The VACUUM command in Delta Lake is designed to remove files that are no longer needed by the table after a certain retention period has passed. This process helps in managing storage efficiently by deleting old data files that are not accessed by any version of the table within the retention period. It does not immediately remove records or encrypt them but instead focuses on cleaning up the storage space by removing file versions that are beyond the specified retention threshold. This ensures that the storage used by Delta Lake tables remains optimized without affecting the data's integrity or availability for queries that might require historical data within the retention period.

**Domain**

Incremental Data Processing

**Question 9Skipped**

A dataset has been defined using Delta Live Tables and includes an expectations clause: CONSTRAINT valid\_timestamp EXPECT (timestamp > '2022-01-01') ON VIOLATION DROP ROW What is the expected behavior when a batch of data containing data that violates these constraints is processed?

**Records that violate the expectation cause the job to fail.** 

**Records that violate the expectation are added to the target dataset and recorded as invalid in the event log.**

**Correct answer**

**Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log.**

**Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset.**

**Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table.**

Overall explanation

In Delta Live Tables, when data does not meet the specified expectations defined by a CONSTRAINT clause, the action defined by ON VIOLATION is executed. In this scenario, the action is DROP ROW, meaning any record that fails to meet the expectation (in this case, records where timestamp is not greater than '2022-01-01') will be removed from the dataset being processed. The details of these violations, including information about the dropped records, are then logged in the event log. This approach ensures data quality by excluding records that do not meet predefined criteria and provides a mechanism for tracking and auditing data quality issues through the event log, without causing the entire job to fail or adding invalid records to the dataset.

**Domain**

Incremental Data Processing

**Question 10Skipped**

Which statement accurately describes the relationship between Bronze tables and raw data?

**Bronze tables contain more accurate data than raw data.**

**Bronze tables contain a less refined view of data than raw data.**

**Bronze tables contain less data than raw data files.**

**Bronze tables contain aggregates while raw data is unaggregated.**

**Correct answer**

**Bronze tables contain raw data with a schema applied.**

Overall explanation

Bronze tables are essentially the initial layer of structured data in a data lakehouse architecture, where raw data is ingested from various sources and a schema is applied to it. This process transforms the raw data, which is often unstructured and in various formats, into a more structured form that can be easily queried and processed further. The key difference between raw data and Bronze tables is not in the quantity or the truthfulness of the data, nor in the refinement or aggregation of the data, but in the application of a schema to the raw data. This schema enables the raw data to be stored in a structured table format, making it more accessible and usable for further processing and analysis steps.

**Domain**

Databricks Lakehouse Platform

**Question 11Skipped**

A data architect has determined that a table of the following format is necessary:

1.  employeeId | startDate | avgRating
2.  a1 | 2009-01-06 | 5.5
3.  a2 | 2018-11-21 | 7.1
4.  …......

Which of the following code blocks uses SQL DDL commands to create an empty Delta table in the above format regardless of whether a table already exists with this name?"

1.  CREATE TABLE table\_name AS
2.  SELECT
3.  employeeId STRING,
4.  startDate DATE,
5.  avgRating FLOAT

**Correct answer**

1.  CREATE OR REPLACE TABLE table\_name (
2.  employeeId STRING,
3.  startDate DATE,
4.  avgRating FLOAT
5.  )
6.  CREATE TABLE IF NOT EXISTS table\_name (
7.  employeeId STRING,
8.  startDate DATE,
9.  avgRating FLOAT
10.  )
11.  CREATE OR REPLACE TABLE table\_name AS
12.  SELECT
13.  employeeId STRING,
14.  startDate DATE,
15.  avgRating FLOAT
16.  USING DELTA
17.  CREATE OR REPLACE TABLE table\_name WITH COLUMNS (
18.  employeeId STRING,
19.  startDate DATE,
20.  avgRating FLOAT
21.  ) USING DELTA

Overall explanation

This SQL command will create a new Delta table with the columns employeeId, startDate, and avgRating, or replace an existing one with the same name, without the need to state USING DELTA explicitly, since it is the assumed default.

**Domain**

Incremental Data Processing

**Question 12Skipped**

How can a data engineer find out who owns "new\_table" if they lack the necessary permissions and don't know the table's owner?

**Correct answer**

**Review the Owner field in the table's page in Data Explorer**

**All of these options can be used to identify the owner of the table**

**Review the Owner field in the table's page in the cloud storage solution**

**There is no way to identify the owner of the table**

**Review the Permissions tab in the table's page in Data Explorer**

Overall explanation

In Databricks, the Data Explorer provides detailed information about tables, including metadata like the owner of the table. The "Owner" field specifically indicates who created or owns the table, making it the direct method to identify the table's owner if you have access to view this information. This assumes that the data engineer has sufficient permissions to view the table's details in the Data Explorer, even if they don't have permissions to modify the table itself.

**Domain**

Data Governance

**Question 13Skipped**

What function in SQL is used to ensure that the array of values returned is composed of unique elements for each group of rows?

**collect\_all**

**array\_distinct**

**Correct answer**

**collect\_set**

**array\_combine**

Overall explanation

The collect\_set function is used to return an array of unique values from a specified column for each group in a GROUP BY clause in SQL. For example, if we have several rows with a 'status' column containing values like \['new', 'active', 'new'\], collect\_set(status) would return \['new', 'active'\], removing the duplicate 'new'.

**Domain**

ELT With Spark SQL and Python

**Question 14Skipped**

A data engineer finds the following code in a project's codebase:

1.  CREATE STREAMING LIVE TABLE loyal\_customers AS
2.  SELECT customer\_id
3.  FROM STREAM(LIVE.customers)
4.  WHERE loyalty\_level = 'high';

Why is the STREAM function used in this query?

**The data in the customers table has been updated since its last run.**

**Correct answer**

**The customers table is a streaming live table.**

**The customers table is a reference to a Structured Streaming query on a PySpark DataFrame.**

**The STREAM function is not needed and will cause an error.**

**The table being created is a live table.**

Overall explanation

The STREAM function in the query indicates that the data is being read from a live, continuously updating source, which is the customers table in this context. This function is specifically used to query streaming data in Delta Live Tables, allowing for real-time data processing and analysis. The presence of STREAM suggests that the customers table is set up as a streaming live table, which is designed to handle data that is continuously ingested and updated. This capability is essential for real-time analytics and processing scenarios, such as identifying loyal customers based on their latest interactions and transactions. The other options do not accurately describe the purpose of the STREAM function within the context of Delta Live Tables and streaming data processing.

**Domain**

Incremental Data Processing

**Question 15Skipped**

Your team is working on a project with a high volume of data transformations and model training tasks. To optimize resource allocation and improve efficiency, which Databricks feature should you use to isolate these tasks in separate environments?

**Utilize Databricks Workspace to organize tasks by project folders.**

**Use Databricks Repos to version control the entire project.**

**Correct answer**

**Implement isolated clusters with different configurations for each task.**

**Create separate notebooks for data transformations and model training.**

Overall explanation

Implementing isolated clusters with different configurations for data transformations and model training tasks allows efficient resource allocation and optimization for each task.

**Domain**

Databricks Lakehouse Platform

**Question 16Skipped**

Why would you consider restarting a Databricks cluster?

**When the cluster's Spark version needs to be downgraded for compatibility reasons.**

**Restarting is required to apply software updates or patches to the cluster.**

**To increase the storage capacity of the cluster's attached storage.**

**Correct answer**

**To apply updates or changes to Spark configurations or cluster-level libraries.**

Overall explanation

Restarting a Databricks cluster is beneficial for applying configuration changes and resolving runtime issues. It ensures that updates to Spark configurations, library installations, or adjustments made to optimize performance are effectively implemented. Additionally, restarting can help clear temporary glitches or memory leaks that affect cluster performance, making it a critical step in maintaining the efficiency and reliability of data processing workflows in Databricks environments.

**Domain**

Databricks Lakehouse Platform

**Question 17Skipped**

A data engineer wants to make a new table from different data sources and change the data a bit. They want to do this in one step without changing the original data. Which SQL command is the best for making a new table and changing the data at the same time?

**Correct answer**

**CREATE TABLE AS SELECT (CTAS)**

**INSERT INTO**

**ALTER TABLE**

**MERGE INTO**

**UPDATE**

Overall explanation

The CREATE TABLE AS SELECT (CTAS) command is perfect for what the data engineer needs. With CTAS, you can make a new table by using the results from a SELECT statement. This lets the engineer pull together data from different places, make some changes, and put it all into a new table, all in one go. This way is quick because it makes a new table and processes data at the same time, and it doesn't mess with the original data. Example of how to do it:

1.  CREATE TABLE new\_table
2.  AS
3.  SELECT
4.  id,
5.  SUM(amount) as total\_amount,
6.  AVG(score) as average\_score
7.  FROM
8.  old\_table
9.  GROUP BY
10.  id;

This example shows how to use CTAS to create a new table called new\_table by adding up and averaging data from old\_table. It's a handy way to make tables for analysis.

**Domain**

Incremental Data Processing

**Question 18Skipped**

Which of the following statements is true about the Workspace Admin role in the Databricks Unity Catalog?

**Workspace Admins are limited to data discovery and cannot alter permissions.**

**Workspace Admins are responsible for creating and deleting metastores only.**

**Workspace Admins cannot manage user permissions or roles.**

**Correct answer**

**Workspace Admins have the ability to manage permissions on workspace assets and change job ownership.**

Overall explanation

Workspace Admins are granted significant control within the workspace, including managing asset permissions, which is essential for the internal governance of the Databricks environment. Their capabilities ensure that the workspace is organized, secure, and operates smoothly by managing user roles and access to various assets.

**Domain**

Data Governance

**Question 19Skipped**

In Databricks, what pattern would you choose to distribute a dataset for parallel processing, such as running multiple transformations or write operations to different tables or systems?

**Funnel**

**Correct answer**

**Fan-out**

**Sequence**

**Iteration**

Overall explanation

The fan-out pattern is optimal for executing parallel processing tasks in Databricks. When a single dataset is to be used by multiple data processing jobs simultaneously, such as applying different transformations or writing to different output systems, the fan-out pattern enables this by distributing the dataset to various tasks without bottlenecking at a single point.

**Domain**

Production Pipelines

**Question 20Skipped**

Given the scenario where a Delta Live Table pipeline includes two datasets defined using STREAMING LIVE TABLE and three datasets defined against Delta Lake table sources using LIVE TABLE, and the table is configured to run in Production mode using the Continuous Pipeline Mode, what is the expected outcome after initiating the pipeline update by clicking Start, assuming unprocessed data exists and all definitions are valid?

**All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will persist to allow for additional testing.**

**All datasets will be updated once and the pipeline will persist without any processing. The compute resources will persist but go unused.**

**Correct answer**

**All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will be deployed for the update and terminated when the pipeline is stopped.**

**All datasets will be updated once and the pipeline will shut down. The compute resources will be terminated.**

**All datasets will be updated once and the pipeline will shut down. The compute resources will persist to allow for additional testing.**

Overall explanation

In Production mode with Continuous Pipeline Mode, the Delta Live Table pipeline is designed for ongoing processing to handle streaming and batch data continuously. This mode ensures that datasets are updated regularly as new data arrives or as changes are detected, without manual intervention. The option indicating that all datasets will be updated at set intervals until the pipeline is shut down, with compute resources being deployed for updates and then terminated upon stopping the pipeline, best matches the operational behavior of Delta Live Tables in Continuous Pipeline Mode. This approach allows for efficient resource management, ensuring that compute resources are utilized for processing as needed and then released, aligning with the principles of cost efficiency and resource optimization in cloud environments.

**Domain**

Incremental Data Processing

**Question 21Skipped**

How can you enable collaborative editing of a Databricks notebook by multiple team members within your organization?

**Correct answer**

**Assign the notebook to a shared folder and grant edit permissions to the team.**

**Set the notebook to "Public" mode, allowing anyone to edit.**

**Email the notebook as an attachment to team members for sequential editing.**

**Use an external version control system to manage changes and synchronize with the Databricks notebook.**

Overall explanation

Placing a notebook in a shared folder within the Databricks workspace and configuring the appropriate access permissions (view or edit) for team members is the most straightforward way to facilitate collaborative editing. This method leverages Databricks' native workspace features to manage access and collaboration efficiently, ensuring that team members can work together on notebooks securely and effectively.

**Domain**

Databricks Lakehouse Platform

**Question 22Skipped**

What is the role of Data Lineage in Databricks’ Data Governance?

**To compress data for efficient storage.**

**To convert data into different formats automatically.**

**To replicate data across multiple cloud providers.**

**Correct answer**

**To capture upstream sources and downstream consumers of data.**

Overall explanation

Data Lineage in Databricks' Data Governance provides visibility into the lifecycle of data, including its origin, what happens to it, and where it moves over time. This is crucial for understanding the data flow, ensuring data quality, and troubleshooting issues that may arise during data processing workflows.

**Domain**

Data Governance

**Question 23Skipped**

A data engineer is evaluating whether to utilize the native versioning feature of Databricks Notebooks or to manage their project with Databricks Repos. What is a benefit of opting for Databricks Repos instead of the built-in versioning of Databricks Notebooks?

**Correct answer**

**Databricks Repos enables working with multiple branches.**

**Databricks Repos allows for commenting on specific alterations.**

**Databricks Repos ensures continuous saving of work in progress.**

**Databricks Repos operates entirely within the Databricks Lakehouse ecosystem.**

**Databricks Repos offers the option to go back to earlier versions of a notebook.**

Overall explanation

The key advantage of using Databricks Repos over the built-in versioning system of Databricks Notebooks is its support for multiple branches. This feature is crucial for collaborative development projects, allowing team members to work on different features or experiments in parallel without affecting the main project. While Databricks Notebooks' versioning allows for reverting to previous versions, Databricks Repos provides a more comprehensive version control system similar to Git, offering functionalities like branching and merging, which are essential for managing complex development workflows.

**Domain**

Databricks Lakehouse Platform

**Question 24Skipped**

A data engineer needs to apply a complex run schedule from one Job to others without manually setting each time in Databricks. Which tool allows them to define and apply this schedule through code (programmatically)?

**datetime**

**There is no way to represent and submit this information programmatically**

**Correct answer**

**Cron syntax**

**pyspark.sql.types.TimestampType**

**pyspark.sql.types.DateType**

Overall explanation

Cron syntax is a powerful tool used to schedule jobs in a Unix/Linux environment, and it's also supported by Databricks for scheduling tasks. Unlike the other options listed, which are data types for representing dates and times in Python and PySpark, Cron syntax allows you to specify complex scheduling patterns in a concise and programmable way. This makes it ideal for a data engineer who wants to automate the scheduling of multiple jobs without having to manually input the schedule each time. Cron syntax can define schedules down to the minute, allowing for highly specific and recurring job schedules, making it the best choice for programmatically transferring complex run schedules to other jobs in Databricks. Example: 0 12 \* \* 1-5 This translates to: "Run at 12:00 PM (noon) on Monday through Friday." Here's a breakdown of this example: The first field 0 represents the minute (at the 0th minute). The second field 12 represents the hour (at 12 PM). The asterisk \* in the third field signifies every day of the month. The asterisk \* in the fourth field signifies every month. The 1-5 in the fifth field represents Monday through Friday (where 1 is Monday and 5 is Friday). Cron syntax allows for complex scheduling to be defined simply and effectively, making it a powerful tool for automating tasks.

**Domain**

Production Pipelines

**Question 25Skipped**

What is the correct way to use Scala and Python code blocks for data processing in the same Databricks notebook?

**Define all variables in Scala and use Python for data processing exclusively.**

**Only Scala can be used in notebooks that require interaction with the Spark engine.**

**Correct answer**

**Use the %scala magic command for Scala cells and %python for Python cells within the notebook.**

**Convert Scala code to Python using a conversion tool before integrating with Python cells.**

Overall explanation

Databricks notebooks support the use of multiple programming languages, including Scala and Python, within the same notebook by utilizing magic commands (%scala for Scala, %python for Python) to denote the language context of each cell. This allows for flexible data processing workflows that leverage the unique capabilities of each language.

**Domain**

Databricks Lakehouse Platform

**Question 26Skipped**

In which of the following file formats is data from Delta Lake tables primarily stored?

**CSV**

**A proprietary, optimized format specific to Databricks**

**Delta**

**Correct answer**

**Parquet**

**JSON**

Overall explanation

Delta Lake tables primarily store data in the Parquet format. Parquet is an open-source, columnar storage file format designed for efficient data storage and retrieval. It offers advantages such as compression and encoding schemes that can significantly reduce storage space and improve I/O efficiency, making it well-suited for complex data processing tasks. Delta Lake leverages Parquet's efficient data storage capabilities while adding a layer of ACID transactions, scalable metadata handling, and schema enforcement, enhancing Parquet's functionality with additional reliability and performance features for big data processing.

**Domain**

Databricks Lakehouse Platform

**Question 27Skipped**

Your organization runs multiple data engineering projects simultaneously, each requiring different cluster configurations and sizes. To optimize resource utilization across these projects, you're considering the use of Cluster Pools. How would Cluster Pools facilitate better resource utilization across multiple projects in this scenario?

**By allowing each project to have its dedicated pool, preventing resource sharing.**

**By providing a fixed number of instances that can only be used by the highest priority project.**

**By locking specific instances to specific projects, based on the project's budget.**

**Correct answer**

**By enabling projects to share a common pool of instances, improving overall resource utilization.**

Overall explanation

In this scenario, Cluster Pools facilitate better resource utilization by enabling multiple projects to share a common pool of pre-warmed instances. This approach ensures that idle resources can be dynamically allocated to different projects as needed, improving efficiency and reducing costs by avoiding the need for each project to maintain its dedicated, potentially underutilized, resources.

**Domain**

Databricks Lakehouse Platform

**Question 28Skipped**

What SQL data type should be used to store an ordered collection of multiple names in a table?

**MAP**

**Correct answer**

**ARRAY**

**VARCHAR**

**STRUCT**

Overall explanation

An ARRAY data type is suitable for storing an arbitrary number of elements of the same data type, which in this case could be a collection of names as strings.

**Domain**

ELT With Spark SQL and Python

**Question 29Skipped**

How does the 'Share' functionality within the Catalog framework in Databricks facilitate collaboration?

**By restricting access to data objects to a single user.**

**Correct answer**

**By enabling data objects in the Catalog to be shared with designated recipients.**

**By creating duplicate copies of data objects for each user.**

**By preventing any sharing of data objects to maintain data isolation.**

Overall explanation

The 'Share' functionality in Databricks' Catalog framework allows for controlled sharing of data objects with specific recipients, which can be other users or systems. This supports collaboration and secure data access management, as data can be shared in a governed manner with appropriate parties.

**Domain**

Data Governance

**Question 30Skipped**

What is a key difference between all-purpose clusters and job clusters in Databricks?

**All-purpose clusters can only run SQL commands, while job clusters can run commands in multiple languages.**

**Correct answer**

**All-purpose clusters are designed for interactive analysis by multiple users, whereas job clusters are optimized for running specific jobs and are terminated after the job is complete.**

**All-purpose clusters do not support autoscaling, whereas job clusters do.**

**Job clusters are permanent and cannot be terminated, unlike all-purpose clusters.**

Overall explanation

The distinction between all-purpose clusters and job clusters lies in their intended use and lifecycle. All-purpose clusters are meant for interactive work, such as exploratory data analysis in notebooks, and can be used by multiple users simultaneously. Job clusters, on the other hand, are created for a specific job or batch process, and they are automatically terminated upon the completion of the job, optimizing resource usage and cost.

**Domain**

Databricks Lakehouse Platform

**Question 31Skipped**

What is a benefit of reusing clusters for multiple tasks in a workflow?

**It eliminates the need for monitoring tasks.**

**Correct answer**

**It reduces cost and startup time.**

**It increases the complexity of the task.**

**It allows for unlimited task execution.**

Overall explanation

Reusing clusters for multiple tasks can significantly reduce costs associated with provisioning new clusters and can also reduce the startup time for tasks, as the cluster is already running and does not need to be initiated from scratch.

**Domain**

Production Pipelines

**Question 32Skipped**

What is the purpose of the explode function in SQL?

**To delete elements from an array field.**

**Correct answer**

**To expand the elements of an array field into separate rows.**

**To combine multiple rows into a single array field.**

**To create a backup of the database.**

Overall explanation

The explode function is used in SQL to take an array (or a map) and create a new row for each element within that array. This is commonly used in data transformation processes where nested data structures are flattened for analysis.

**Domain**

ELT With Spark SQL and Python

**Question 33Skipped**

A data engineer needs to add a new data record to an existing Delta table named my\_table. The record details are as follows:

1.  id STRING = 'c01'
2.  rank INTEGER = 9
3.  rating FLOAT = 6.2

Which SQL command should be used to append this new record to my\_table?

**Correct answer**

**INSERT INTO my\_table VALUES ('c01', 9, 6.2)**

**INSERT VALUES ('c01', 9, 6.2) INTO my\_table**

**UPDATE VALUES ('c01', 9, 6.2) my\_table**

**my\_table UNION VALUES ('c01', 9, 6.2)**

**UPDATE my\_table VALUES ('c01', 9, 6.2)**

Overall explanation

The INSERT INTO statement is the appropriate SQL command for adding a new row of data to an existing table. The syntax INSERT INTO my\_table VALUES ('c01', 9, 6.2) correctly specifies the table name (my\_table) and the values for the new record, aligning with the table's schema. This command is designed to append a new record to the table, effectively adding the specified data as a new row. This method is direct and efficiently fulfills the requirement to incorporate a new record into the table.

**Domain**

Incremental Data Processing

**Question 34Skipped**

Consider a column of JSON strings that needs to be converted to structs. Which SQL command creates a new table to store these structs with no null fields, based on a schema generated from an example JSON string?

**CREATE TABLE parsed\_data AS**

**SELECT FROM\_JSON(json\_column) AS struct\_column**

**FROM original\_table;**

**Correct answer**

**CREATE OR REPLACE TABLE parsed\_events AS**

**SELECT FROM\_JSON(json\_column, schema\_of\_json('<example\_json>')) AS struct\_column FROM original\_table;**

**CREATE TABLE parsed\_data AS**

**SELECT CAST(json\_column AS STRUCT)**

**FROM original\_table;**

**CREATE TABLE parsed\_data AS**

**SELECT TO\_STRUCT(json\_column)**

**FROM original\_table;**

Overall explanation

CREATE OR REPLACE TABLE parsed\_events AS SELECT FROM\_JSON(json\_column, schema\_of\_json('<example\_json>')) AS struct\_column FROM original\_table;

**Domain**

ELT With Spark SQL and Python

**Question 35Skipped**

A data engineer aims to implement custom logic on the string column city in the locaion table for a particular scenario. To efficiently apply this custom logic across the dataset, the engineer plans to establish a SQL user-defined function (UDF). Which code snippet correctly defines this SQL UDF?

1.  CREATE UDF combine\_california (city STRING)
2.  RETURN CASE
3.  WHEN city = "San Francisco" THEN "California"
4.  ELSE city
5.  END;
6.  CREATE FUNCTION combine\_california (city STRING)
7.  RETURN CASE
8.  WHEN city = "San Francisco" THEN "California"
9.  ELSE city
10.  END;
11.  CREATE FUNCTION combine\_california (city STRING)
12.  RETURNS STRING
13.  CASE
14.  WHEN city = "San Francisco" THEN "California"
15.  ELSE city
16.  END;

**Correct answer**

1.  CREATE FUNCTION combine\_california (city STRING)
2.  RETURNS STRING
3.  RETURN CASE
4.  WHEN city = "San Francisco" THEN "California"
5.  ELSE city
6.  END;
7.  CREATE UDF combine\_california (city STRING)
8.  RETURN CASE
9.  WHEN city = "San Francisco" THEN "California"
10.  ELSE city
11.  END;

Overall explanation

The correct syntax for creating a user-defined function (UDF) in SQL involves specifying the CREATE FUNCTION statement, followed by the function name and parameters. It is crucial to define the return type with RETURNS STRING and to use the RETURN keyword to specify the output of the function based on the input conditions. This code block correctly follows the SQL syntax for creating a UDF, including the use of CASE for conditional logic within the function. The other options either misuse SQL syntax for creating UDFs, such as incorrectly using CREATE UDF instead of CREATE FUNCTION, or omit necessary parts of the syntax like RETURNS STRING or the correct use of RETURN before the CASE statement.

**Domain**

ELT With Spark SQL and Python

**Question 36Skipped**

How does Databricks' workflow feature 'Simple Workflow Authoring' cater to teams?

**It is exclusively available for users with administrative privileges.**

**It requires advanced programming skills for all data team members.**

**It only allows workflow editing in command-line interfaces.**

**Correct answer**

**It provides a point-and-click authoring experience accessible to all team members.**

Overall explanation

Simple Workflow Authoring' in Databricks is designed to be user-friendly, offering a point-and-click experience that enables all data team members to create and manage workflows, regardless of their technical expertise. This inclusivity promotes collaboration and allows for broader contribution from team members with various skill levels.

**Domain**

Production Pipelines

**Question 37Skipped**

How can you run one Databricks notebook from within another notebook?

**By appending the %run command with the notebook's URL.**

**By using the %run command followed by the path to the notebook stored in DBFS.**

**Correct answer**

**By using the %run command followed by the relative or absolute path to the notebook within the Databricks workspace.**

**By preceding each cell in the target notebook with %run to execute its code.**

Overall explanation

The %run magic command in Databricks allows for the execution of another notebook by specifying its path, which can be either relative to the current notebook's location or an absolute path within the Databricks workspace.

**Domain**

Databricks Lakehouse Platform

**Question 38Skipped**

A data engineering team is tasked with automating a Spark SQL query that compiles monthly sales data. The query is designed to access a table named in the format monthly\_sales\_YYYYMM, where YYYYMM denotes the year and month of the sales data. The automation needs to ensure that each execution of the query targets the table for the month preceding the current one. 

The standard query format is:

1.  SELECT product\_category, SUM(sales)
2.  FROM monthly\_sales\_YYYYMM
3.  GROUP BY product\_category;

Considering the requirement for the query to adapt monthly to access data from the previous month, which strategy should the data engineering team employ?    For instance, if the current month is March 2023, the query should automatically target the table named monthly\_sales\_202302.

**Revise the database design to consolidate sales data into a single table with a month column, avoiding the necessity for separate monthly tables.**

**Correct answer**

**Develop a PySpark script that calculates the date for the previous month and dynamically inserts this date into the table name within the query string.**

**Propose altering the query's execution frequency to quarterly, thereby reducing the need for monthly table name updates.**

**Manually update the table name in the query to reflect the previous month's data before executing it each time.**

Overall explanation

Utilizing PySpark in conjunction with Python's datetime functionality allows the data engineering team to automate the calculation of the previous month's date. This approach enables the dynamic construction of the query string to include the accurate table name for the month preceding the current one. For example, if the script runs in March 2023, it automatically adjusts the query to target monthly\_sales\_202302, ensuring the query remains up-to-date without manual adjustments. This method is efficient, reduces the risk of human error, and ensures timely access to the relevant monthly sales data.

**Domain**

ELT With Spark SQL and Python

**Question 39Skipped**

In which of the following scenarios should a data engineer use the MERGE INTO command instead of the INSERT INTO command?

**When the location of the data needs to be changed**

**When the source table can be deleted**

**When the source is not a Delta table**

**When the target table is an external table**

**Correct answer**

**When the target table cannot contain duplicate records**

Overall explanation

The MERGE INTO command is used when a data engineer needs to ensure that the target table does not contain duplicate records. It allows for efficient upsert operations, enabling updates, inserts, and deletes while maintaining data integrity. This command is particularly valuable in scenarios where preventing duplicate entries in the target table is crucial, ensuring that data remains accurate and consistent. Other factors, such as the source table being deletable or the location of the data, are not as critical in determining the use of MERGE INTO as the requirement to manage duplicates.

**Domain**

Incremental Data Processing

**Question 40Skipped**

Which job uses a Gold table as its source?

**A job that ingests raw data from a streaming source into the Lakehouse**

**Correct answer**

**A job that queries aggregated data designed to feed into a dashboard**

**A job that enriches data by parsing its timestamps into a human-readable format**

**A job that cleans data by removing malformatted records**

**A job that aggregates uncleaned data to create standard summary statistics**

Overall explanation

The job that queries aggregated data designed to feed into a dashboard is the correct choice because it directly aligns with the purpose of Gold tables in data architecture. Gold tables are the culmination of data processing efforts, containing data that has been meticulously cleaned, transformed, and aggregated. This level of readiness is essential for analytics and reporting, where the accuracy and integrity of data underpin critical business decisions. Such a job requires data that is primed for high-level analysis, embodying the characteristics of Gold tables: reliability, completeness, and analytical readiness. Unlike tasks focused on data preparation or ingestion, querying for dashboard insights demands the refined, quality-assured data found in Gold tables, making this job the ideal use case.

**Domain**

Databricks Lakehouse Platform

**Question 41Skipped**

Which of the following commands will return the number of null values in the member\_id column?

**SELECT count\_null(member\_id) FROM my\_table;**

**Correct answer**

**SELECT count\_if(member\_id IS NULL) FROM my\_table;**

**SELECT count(member\_id) - count\_null(member\_id) FROM my\_table;**

**SELECT count(member\_id) FROM my\_table;**

**SELECT null(member\_id) FROM my\_table;**

Overall explanation

The count\_if function along with the condition member\_id IS NULL to count the number of null values in the member\_id column of the my\_table. This is the standard way to count null values in SQL queries.

**Domain**

ELT With Spark SQL and Python

**Question 42Skipped**

Which of the following queries is performing a streaming hop from raw data to a Bronze table?

1.  (spark.table("sales")
2.  .withColumn("avgPrice", col("sales") / col("units"))
3.  .writeStream
4.  .option("checkpointLocation", checkpointPath)
5.  .outputMode("append")
6.  .table("newSales")
7.  )
8.  (spark.table("sales")
9.  .filter(col("units") > 0)
10.  .writeStream
11.  .option("checkpointLocation", checkpointPath)
12.  .outputMode("append")
13.  .table("newSales")
14.  )
15.  (spark.table("sales")
16.  .groupBy("store")
17.  .agg(sum("sales"))
18.  .writeStream
19.  .option("checkpointLocation", checkpointPath)
20.  .outputMode("complete")
21.  .table("newSales")
22.  )
23.  (spark.read.load(rawSalesLocation)
24.  .write
25.  .mode("append")
26.  .table("newSales")
27.  )

**Correct answer**

1.  (spark.readStream.load(rawSalesLocation)
2.  .writeStream
3.  .option("checkpointLocation", checkpointPath)
4.  .outputMode("append")
5.  .table("newSales")
6.  )

Overall explanation

The code performs a streaming read from a raw data source and writes to a new table using append mode, consistent with the Bronze table's role in continuously capturing and storing raw data in a structured format without extensive transformation.

**Domain**

Incremental Data Processing

**Question 43Skipped**

You have a scheduled weekly job that compiles analytics reports. Stakeholders depend on these reports for their Monday morning meetings. It's crucial that any job failures are addressed promptly, and stakeholders are updated on the status of their reports. How should you set up the job to ensure stakeholders are promptly informed about the status of the analytics report job runs?

**Disable all alerts to avoid flooding stakeholders with notifications.**

**Schedule the job run for Monday morning and hope it completes on time.**

**Manually check the job status every Monday and send emails to stakeholders.**

**Correct answer**

**Configure the job to send completion alerts to the stakeholders' email addresses.**

Overall explanation

By setting up completion alerts directed to the stakeholders' email addresses, you ensure they are automatically and promptly informed about the status of the job runs. This allows stakeholders to have the latest information for their meetings without manual intervention from the data team.

**Domain**

Production Pipelines

**Question 44Skipped**

A data engineer tries to remove a Spark SQL table named my\_table with the intention of erasing both the table's metadata and its data. They execute the command: DROP TABLE IF EXISTS my\_table; After running this command, my\_table no longer shows up in the list of tables, but the data files remain. What explains the persistence of the data files despite the deletion of the metadata?

**The table was a managed table.**

**The table's data exceeded 10 GB**

**Correct answer**

**The table was set up as an external table.**

**The table was specified without a location.**

**The table's data was under 10 GB.**

Overall explanation

In Spark SQL, when you drop a managed table, Spark deletes both the table's metadata and its data files. However, if the table is an external table, dropping it only removes the metadata, leaving the data files untouched. This behavior ensures that you don't accidentally lose data that might be shared with other tables or applications. The fact that the data files still exist after dropping the table suggests that it was defined as an external table, which is why only the metadata was deleted upon executing the DROP TABLE command. Managed tables, on the other hand, store both data and metadata within Spark's control, leading to both being deleted when the table is dropped.

**Domain**

ELT With Spark SQL and Python

**Question 45Skipped**

In SQL queries that involve file data, how are text and binary files processed differently?

**Correct answer**

**Text files generate a column with a string value for each line, while binary files have columns for path, modification time, length, and content.**

**Text and binary files both generate columns for path, modification time, length, and content.**

**Text files create a single column for each byte of data, whereas binary files create a column for each bit.**

**Binary files generate a column with a string value for each line, whereas text files have columns for path, modification time, length, and content.**

Overall explanation

When querying file-based data, text files are generally processed with each line representing a string value in a single column. In contrast, binary files are handled differently, with a schema that includes metadata such as the file path, modification time, file length, and the content itself.

**Domain**

ELT With Spark SQL and Python