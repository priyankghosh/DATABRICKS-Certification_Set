**Question 1Skipped**

What function in SQL is used to ensure that the array of values returned is composed of unique elements for each group of rows?

**Correct answer**

**collect\_set**

**array\_distinct**

**array\_combine**

**collect\_all**

Overall explanation

The collect\_set function is used to return an array of unique values from a specified column for each group in a GROUP BY clause in SQL. For example, if we have several rows with a 'status' column containing values like \['new', 'active', 'new'\], collect\_set(status) would return \['new', 'active'\], removing the duplicate 'new'.

**Domain**

ELT With Spark SQL and Python

**Question 2Skipped**

How would you define a column in a SQL table that includes multiple attributes like street, city, and zip code?

**As a MAP with key-value pairs**

**Correct answer**

**As a STRUCT with nested attributes**

**As separate VARCHAR columns**

**As a single TEXT column with concatenated values**

Overall explanation

A STRUCT data type allows for the creation of an ordered collection of columns with potentially different data types. This makes it suitable for defining complex data such as an address, which consists of multiple attributes like street, city, and zip code.

**Domain**

ELT With Spark SQL and Python

**Question 3Skipped**

A data engineering team is tasked with automating a Spark SQL query that compiles monthly sales data. The query is designed to access a table named in the format monthly\_sales\_YYYYMM, where YYYYMM denotes the year and month of the sales data. The automation needs to ensure that each execution of the query targets the table for the month preceding the current one. The standard query format is:

1.  SELECT product\_category, SUM(sales)
2.  FROM monthly\_sales\_YYYYMM
3.  GROUP BY product\_category;

Considering the requirement for the query to adapt monthly to access data from the previous month, which strategy should the data engineering team employ? For instance, if the current month is March 2023, the query should automatically target the table named monthly\_sales\_202302.

**Propose altering the query's execution frequency to quarterly, thereby reducing the need for monthly table name updates.**

**Correct answer**

**Develop a PySpark script that calculates the date for the previous month and dynamically inserts this date into the table name within the query string.**

**Manually update the table name in the query to reflect the previous month's data before executing it each time.**

**Revise the database design to consolidate sales data into a single table with a month column, avoiding the necessity for separate monthly tables.**

Overall explanation

Utilizing PySpark in conjunction with Python's datetime functionality allows the data engineering team to automate the calculation of the previous month's date. This approach enables the dynamic construction of the query string to include the accurate table name for the month preceding the current one. For example, if the script runs in March 2023, it automatically adjusts the query to target monthly\_sales\_202302, ensuring the query remains up-to-date without manual adjustments. This method is efficient, reduces the risk of human error, and ensures timely access to the relevant monthly sales data.

**Domain**

ELT With Spark SQL and Python

**Question 4Skipped**

What distinguishes a materialized view from a standard database view?

**Correct answer**

**A materialized view stores the output of a query physically on disk, whereas a standard view does not store any data but provides a virtual view based on a query.**

**A materialized view can only be used in relational databases, while a standard view is specific to NoSQL databases.**

**A materialized view automatically updates in real-time as the underlying data changes, whereas a standard view requires manual refresh.**

**There is no difference; both are terms used interchangeably in the context of database management.**

Overall explanation

The key difference lies in the physical storage of query results for materialized views, which enhances performance for read-heavy operations, unlike standard views that dynamically execute the underlying query each time they are accessed.

**Domain**

ELT With Spark SQL and Python

**Question 5Skipped**

Which syntax is used in Spark SQL to access subfields in JSON strings?

**Correct answer**

**Colon (:)**

**Semicolon (;)**

**Comma (,)**

**Dot (.)**

Overall explanation

In Spark SQL, the colon syntax is specifically used to traverse and access subfields within JSON strings. This syntax allows for querying nested JSON data directly.

**Domain**

ELT With Spark SQL and Python

**Question 6Skipped**

A data engineer is tasked with constructing a table in Databricks, sourcing data from the organization's pre-existing SQLite database. To accomplish this, they execute the command below:

1.  CREATE TABLE jdbc\_customer360
2.  USING \_\_\_\_\_\_\_\_\_\_\_\_\_
3.  OPTIONS (
4.  url "jdbc:sqlite:/customers.db",
5.  datatable "customer360"
6.  )

Which option correctly completes the command to fulfill this requirement?

**org.apache.spark.sql.sqlite**

**Correct answer**

**org.apache.spark.sql.jdbc**

**sqlite**

**autoloader**

**DELTA**

Overall explanation

The correct way to specify the data source for creating a table in Databricks that connects to an external database via JDBC is to use the fully qualified JDBC class name. In this case, org.apache.spark.sql.jdbc is the appropriate format for specifying the JDBC source for Spark SQL commands. This option allows Spark to use the JDBC connector to interact with external databases, including SQLite, by providing the JDBC URL and other connection options. The USING clause in the CREATE TABLE command must be followed by the data source format that Spark understands for establishing the connection, making org.apache.spark.sql.jdbc the correct choice for integrating with an SQLite database via JDBC.

**Domain**

ELT With Spark SQL and Python

**Question 7Skipped**

Given the task of dynamically generating a table name in Python using variables for region, store, and year, where the desired format for the table name is akin to "la200\_sales\_2022" for region="la", store="200", and year="2022", which Python command would correctly construct this table name?

**"{region}+{store}+\_sales\_+{year}"**

**Correct answer**

**f"{region}{store}\_sales\_{year}"**

**"{region}{store}\_sales\_{year}"**

**f"{region}+{store}+\_sales\_+{year}"**

Overall explanation

To dynamically create a string in Python that incorporates variables directly into the string's content, the f-string syntax (f"") is used. This method allows for embedding expressions inside string literals efficiently. The correct format, f"{region}{store}\_sales\_{year}", seamlessly integrates the variables region, store, and year with the static string parts to generate the desired table name. This approach is straightforward and eliminates the need for concatenation operators (+), providing a clean and readable solution for string construction with variable content.

**Domain**

ELT With Spark SQL and Python

**Question 8Skipped**

Assuming you have a Delta Lake table named customer\_purchases that contains duplicate records based on customer\_id. You want to deduplicate these records by keeping only the latest purchase record for each customer based on a purchase\_date. How would you use the MERGE statement in Databricks SQL to achieve this?

1.  MERGE INTO customer\_purchases AS target
2.  USING customer\_purchases AS source
3.  ON target.customer\_id = source.customer\_id
4.  WHEN MATCHED AND target.purchase\_date < source.purchase\_date THEN DELETE;
5.  MERGE INTO customer\_purchases AS target
6.  USING (
7.  SELECT customer\_id, MAX(purchase\_date) AS latest\_purchase\_date
8.  FROM customer\_purchases
9.  GROUP BY customer\_id
10.  ) AS source
11.  ON target.customer\_id = source.customer\_id
12.  WHEN MATCHED AND target.purchase\_date < source.latest\_purchase\_date THEN DELETE
13.  WHEN NOT MATCHED THEN INSERT \*;

**Correct answer**

1.  MERGE INTO customer\_purchases AS target
2.  USING (
3.  SELECT customer\_id, MAX(purchase\_date) AS latest\_purchase\_date
4.  FROM customer\_purchases
5.  GROUP BY customer\_id
6.  ) AS source
7.  ON target.customer\_id = source.customer\_id AND target.purchase\_date < source.latest\_purchase\_date
8.  WHEN MATCHED THEN DELETE;
9.  MERGE INTO customer\_purchases AS target
10.  USING (
11.  SELECT customer\_id, purchase\_date
12.  FROM customer\_purchases
13.  WHERE purchase\_date = (SELECT MAX(purchase\_date) FROM customer\_purchases GROUP BY customer\_id)
14.  ) AS source
15.  ON target.customer\_id = source.customer\_id
16.  WHEN MATCHED THEN UPDATE SET target.\* = source.\*
17.  WHEN NOT MATCHED THEN INSERT \*;

Overall explanation

This option correctly identifies the use of MERGE for deduplication in Delta Lake. It merges the customer\_purchases table with a subquery that selects the latest purchase\_date for each customer\_id. The ON clause matches records in the target table with those in the source based on customer\_id and where the target's purchase\_date is less than the source's latest\_purchase\_date, indicating that there are newer records. The WHEN MATCHED THEN DELETE clause ensures that only the most recent purchase record for each customer is retained, effectively deduplicating the table based on the specified criteria.

**Domain**

ELT With Spark SQL and Python

**Question 9Skipped**

A data engineer aims to implement custom logic on the string column city in the locaion table for a particular scenario. To efficiently apply this custom logic across the dataset, the engineer plans to establish a SQL user-defined function (UDF). Which code snippet correctly defines this SQL UDF?

1.  CREATE UDF combine\_california (city STRING)
2.  RETURN CASE
3.  WHEN city = "San Francisco" THEN "California"
4.  ELSE city
5.  END;
6.  CREATE FUNCTION combine\_california (city STRING)
7.  RETURN CASE
8.  WHEN city = "San Francisco" THEN "California"
9.  ELSE city
10.  END;
11.  CREATE FUNCTION combine\_california (city STRING)
12.  RETURNS STRING
13.  CASE
14.  WHEN city = "San Francisco" THEN "California"
15.  ELSE city
16.  END;

**Correct answer**

1.  CREATE FUNCTION combine\_california (city STRING)
2.  RETURNS STRING
3.  RETURN CASE
4.  WHEN city = "San Francisco" THEN "California"
5.  ELSE city
6.  END;
7.  CREATE UDF combine\_california (city STRING)
8.  RETURN CASE
9.  WHEN city = "San Francisco" THEN "California"
10.  ELSE city
11.  END;

Overall explanation

The correct syntax for creating a user-defined function (UDF) in SQL involves specifying the CREATE FUNCTION statement, followed by the function name and parameters. It is crucial to define the return type with RETURNS STRING and to use the RETURN keyword to specify the output of the function based on the input conditions. This code block correctly follows the SQL syntax for creating a UDF, including the use of CASE for conditional logic within the function. The other options either misuse SQL syntax for creating UDFs, such as incorrectly using CREATE UDF instead of CREATE FUNCTION, or omit necessary parts of the syntax like RETURNS STRING or the correct use of RETURN before the CASE statement.

**Domain**

ELT With Spark SQL and Python

**Question 10Skipped**

When querying data from a CSV file in Databricks SQL, what prefix should be included after the FROM keyword to correctly interpret the source as a CSV file?

**Correct answer**

**csv**

**Explanation**

The prefix "csv" after the FROM keyword is the correct way to interpret a CSV file as a data source in Databricks SQL. It is case-sensitive and should be written in lowercase to correctly identify the file format.

**CSV**

**Explanation**

Using "CSV" as a prefix after the FROM keyword is not the correct way to interpret a CSV file as a data source in Databricks SQL. The correct prefix is case-sensitive and should be lowercase.

**TEXT**

**Explanation**

Using "TEXT" as a prefix after the FROM keyword is not the standard way to interpret a CSV file as a data source in Databricks SQL. The correct prefix for CSV files is different and specific to the file format.

**FILE**

**Explanation**

The prefix "FILE" after the FROM keyword is not the standard way to interpret a CSV file as a data source in Databricks SQL. The correct prefix for CSV files is different.

Overall explanation

To correctly query a CSV file in Databricks SQL, the prefix **csv** is essential for indicating the file format. Databricks supports querying CSV files using a function like read\_files, which accurately interprets the CSV's structure, handling headers, delimiters, and schema inferences if needed. This method ensures that the file's data is read properly, aligning with Databricks' best practices for handling data formats.

**Domain**

ELT With Spark SQL and Python

**Question 11Skipped**

When querying data from a JSON file in Databricks SQL, what prefix should be included after the FROM keyword?

**FILE**

**JSON**

**TEXT**

**Correct answer**

**json**

Overall explanation

To query data directly from JSON files using Databricks SQL, the prefix json is used after the FROM keyword, followed by the path to the JSON file. This indicates that the query should interpret the source data as JSON format, enabling SQL-like querying capabilities on semi-structured JSON data.

**Domain**

ELT With Spark SQL and Python

**Question 12Skipped**

How can you use the count\_if function to count the number of rows in a table sales where the amount column is greater than 100?

**SELECT COUNT\_IF(amount > 100) AS count\_above\_100 FROM sales;**

**SELECT COUNT(\*) FROM sales WHERE amount > 100;**

**Correct answer**

**SELECT COUNT\_IF(amount > 100) FROM sales;**

**SELECT COUNT(amount > 100) FROM sales;**

Overall explanation

The count\_if function is specifically designed for conditional counting within a SELECT statement. It allows for counting rows where a specified condition is true without needing additional filtering with a WHERE clause.

**Domain**

ELT With Spark SQL and Python

**Question 13Skipped**

A data engineering team is experiencing slow query performance when extracting specific rows from a Delta table, where the relevant rows are dispersed across the data files. Despite optimizing the data file sizes, the issue persists. Which optimization strategy could enhance the query's speed under these circumstances?

**Correct answer**

**Z-Ordering**

**Data skipping**

**Bin-packing**

**Writing as a Parquet file**

Overall explanation

Z-Ordering is a technique that reorders the rows of a table to bring related items closer together physically on the disk. This is particularly useful when queries filter on multiple columns, as it reduces the amount of data scanned by co-locating related information in the same set of files. In the scenario described, where the rows meeting the query condition are scattered throughout the data files, Z-Ordering can significantly improve query performance by minimizing the data that needs to be read. Data skipping and bin-packing are also optimization techniques, but they serve different purposes. Data skipping is about skipping irrelevant data based on statistics, and bin-packing optimizes file size and layout without specifically addressing the issue of dispersed data. Writing as a Parquet file and tuning the file size are general optimizations that do not directly address the problem of efficiently accessing sparsely located rows.

**Domain**

ELT With Spark SQL and Python

**Question 14Skipped**

A data engineering team at a retail company uses Databricks for analyzing sales data. The team needs to generate a weekly report that includes insights into sales performance across various regions, with data aggregated from millions of transactions stored in Delta Lake. The query to generate this report is complex and time-consuming, leading to delays. To improve efficiency, the team decides to implement a strategy that mirrors the concept of a materialized view within the Databricks environment. Which of the following strategies would best achieve this objective?

**Store the query results in a blob storage and update it manually every month to ensure data freshness.**

**Directly query the transactional data each time the report is needed, ensuring the most up-to-date information is used.**

**Increase the compute resources of the Databricks cluster before running the weekly report to reduce execution time.**

**Correct answer**

**Create a new Delta Lake table that pre-aggregates the sales data on a daily basis and query this table for the weekly report.**

Overall explanation

By pre-aggregating the sales data into a new Delta Lake table, the team effectively creates a solution similar to a materialized view. This approach significantly reduces the time required to generate the weekly report by avoiding the need to process the entire dataset each time. The pre-aggregated table can be refreshed on a scheduled basis (e.g., daily) using Databricks Jobs, ensuring that the report includes relatively up-to-date information while optimizing the query performance. This strategy aligns with data engineering best practices in Databricks by leveraging Delta Lake's capabilities for managing and processing large datasets efficiently.

**Domain**

ELT With Spark SQL and Python

**Question 15Skipped**

Which of the following SQL keywords can be used to convert a table from a long format to a wide format?

**TRANSFORM**

**WHERE**

**SUM**

**CONVERT**

**Correct answer**

**PIVOT**

Overall explanation

The PIVOT SQL keyword is specifically designed for transforming data from a long format to a wide format. This operation allows for the rotation of data, turning unique values from one column into multiple columns in the output, effectively summarizing and reorganizing the data. It's particularly useful for creating summary reports and for data analysis purposes, where seeing data in a wide format can provide more insights at a glance than the long format.

**Domain**

ELT With Spark SQL and Python

**Question 16Skipped**

What is the primary use of the PIVOT clause in Spark SQL?

**To filter rows based on a specified condition**

**To merge multiple rows into a single row by creating a wider table with more columns**

**To split a single column into multiple columns based on column values**

**Correct answer**

**To perform aggregation and convert rows into columns, transforming data from a long format to a wide format**

Overall explanation

The PIVOT clause in Spark SQL is used to perform row-to-column transformations, where it aggregates data and rotates rows into columns, effectively converting data from a long format (where data is presented in rows) to a wide format (where data is presented in columns). This is particularly useful for creating summary tables and for data analysis purposes.

**Domain**

ELT With Spark SQL and Python

**Question 17Skipped**

A data engineer has a Python variable table\_name that they would like to use in a SQL query. They want to construct a Python code block that will run the query using table\_name. They have the following incomplete code block: \_\_\_\_(f"SELECT customer\_id, spend FROM {table\_name}") Which of the following can be used to fill in the blank to successfully complete the task?

**Correct answer**

**spark.sql**

**spark.table**

**dbutils.sql**

**spark.delta.sql**

**spark.delta.table**

Overall explanation

Using spark.sql within Python code offers a flexible and powerful way to leverage SQL capabilities while working within a Python-centric data processing workflow.

**Domain**

ELT With Spark SQL and Python

**Question 18Skipped**

A data engineer is working on a project that involves analyzing a large dataset using Spark SQL. To facilitate the analysis, they create a temporary view of the dataset for preliminary exploration. After creating the temporary view in their current Spark session, they attempt to access this view from a different Spark session for further analysis. Why is the data engineer unable to access the temporary view from a different Spark session?

**Temporary views require special permissions to be accessed from different sessions.**

**The data engineer did not correctly create the temporary view, resulting in an error when trying to access it from another session.**

**Correct answer**

**Temporary views in Spark SQL are session-scoped and cannot be accessed from different sessions.**

**The network connection between the two Spark sessions was lost, preventing access to the temporary view.**

**Temporary views are automatically deleted after a certain period, and the time limit was exceeded.**

Overall explanation

Temporary views in Spark SQL are designed to be session-scoped, meaning they only exist for the duration of the session in which they were created. Once the session ends or if an attempt is made to access the temporary view from a different session, the view will not be available. This behavior is by design, as temporary views are intended for transient, session-specific tasks such as exploratory data analysis or temporary data manipulation. If a view needs to be accessed across multiple sessions, a global temporary view should be used instead. This limitation ensures that temporary views do not consume resources beyond their intended scope and do not interfere with other sessions' operations.

**Domain**

ELT With Spark SQL and Python

**Question 19Skipped**

What is the purpose of the explode function in SQL?

**To delete elements from an array field.**

**To create a backup of the database.**

**To combine multiple rows into a single array field.**

**Correct answer**

**To expand the elements of an array field into separate rows.**

Overall explanation

The explode function is used in SQL to take an array (or a map) and create a new row for each element within that array. This is commonly used in data transformation processes where nested data structures are flattened for analysis.

**Domain**

ELT With Spark SQL and Python

**Question 20Skipped**

Which function is typically used to convert a JSON string into a struct in SQL?

**TO\_STRUCT**

**JSON\_TO\_STRUCT**

**Correct answer**

**FROM\_JSON**

**CAST\_AS\_STRUCT**

Overall explanation

The FROM\_JSON function is commonly used in SQL to parse a JSON string and convert it into a struct data type, allowing for access to the data as if it were a regular row in a table.

**Domain**

ELT With Spark SQL and Python

**Question 21Skipped**

A data engineer aims to completely remove a Spark SQL table named my\_table, including all associated metadata and data files. They execute the following command: DROP TABLE IF EXISTS my\_table; After this operation, my\_table is not listed in the output of SHOW TABLES, and they verify that the data files have also been deleted. What is the reason both the table's metadata and data files were successfully deleted?

**The table was specified without a location.**

**The table was set up as an external table.**

**Correct answer**

**The table was a managed table.**

**The table's data exceeded 10 GB.**

**The table's data was under 10 GB.**

Overall explanation

In Spark SQL, dropping a managed table results in the deletion of both the table's metadata and its associated data files. This is because managed tables are fully controlled by Spark, which includes managing the storage of data files. When a managed table is dropped, Spark ensures that all resources associated with the table, including data files stored in the warehouse directory, are also removed. This behavior contrasts with external tables, where dropping the table would only remove the metadata, leaving the data files intact since they are managed outside of Spark's control. The complete removal of both metadata and data files in this scenario indicates that my\_table was indeed a managed table.

**Domain**

ELT With Spark SQL and Python

**Question 22Skipped**

A data engineer needs to decide between using a temporary view and implementing a strategy akin to a materialized view for a data transformation task. The task involves aggregating large volumes of data from various sources to create a comprehensive dashboard. The dashboard needs to be updated daily with data that has minimal latency. Which of the following options best describes the appropriate use case for each approach?

**Correct answer**

**Implement a materialized view-like strategy for the dashboard to ensure quick access to pre-aggregated data, and use a temporary view for ad-hoc, real-time analysis tasks that do not require data persistence.**

**Use a temporary view for the dashboard to ensure data is always up-to-date with real-time transformations, and implement a materialized view-like strategy for historical data analysis where data latency is acceptable.**

**Implement a materialized view-like strategy for both the dashboard and historical data analysis to reduce the computational load on the Databricks cluster.**

**Use a temporary view for both the dashboard and historical data analysis to maximize data freshness and minimize storage costs.**

Overall explanation

A materialized view-like strategy, such as creating pre-aggregated Delta tables, is ideal for the dashboard because it provides quick access to data that does not change frequently, reducing the load on the cluster and improving response times for end-users. Temporary views are better suited for real-time, ad-hoc analysis tasks where the data does not need to be stored persistently, allowing for flexibility and immediate insights without affecting storage costs or requiring data refresh mechanisms. This approach leverages the strengths of both strategies to meet different needs within the Databricks environment.

**Domain**

ELT With Spark SQL and Python

**Question 23Skipped**

In SQL queries that involve file data, how are text and binary files processed differently?

**Binary files generate a column with a string value for each line, whereas text files have columns for path, modification time, length, and content.**

**Correct answer**

**Text files generate a column with a string value for each line, while binary files have columns for path, modification time, length, and content.**

**Text and binary files both generate columns for path, modification time, length, and content.**

**Text files create a single column for each byte of data, whereas binary files create a column for each bit.**

Overall explanation

When querying file-based data, text files are generally processed with each line representing a string value in a single column. In contrast, binary files are handled differently, with a schema that includes metadata such as the file path, modification time, file length, and the content itself.

**Domain**

ELT With Spark SQL and Python

**Question 24Skipped**

What is the purpose of using schema\_of\_json in SQL?

**To validate the structure of a JSON string.**

**Correct answer**

**To generate a schema that can be used to parse a JSON string into a struct.**

**To compress the JSON data for storage efficiency.**

**To create a new JSON string from a struct.**

Overall explanation

schema\_of\_json is used to generate a schema based on an example JSON string, which defines the structure that FROM\_JSON will use to parse JSON strings into structs with the appropriate data types.

**Domain**

ELT With Spark SQL and Python

**Question 25Skipped**

Which benefit is offered by utilizing array functions in Spark SQL?

**The capability to manipulate data across specific partitions and window frames.**

**The capability to interact with a collection of tables for streamlined automation.**

**Correct answer**

**The capability to process complex, hierarchical data structures sourced from JSON documents.**

**The capability to handle multiple data types simultaneously.**

**The capability to manage time-based data over defined periods.**

Overall explanation

Array functions in Spark SQL are specifically designed to enhance the handling and analysis of complex data types, such as those found in nested arrays or structures typical of JSON files. These functions allow for the manipulation of elements within an array, enabling data engineers and scientists to efficiently process and analyze data that is structured in a hierarchical manner. This is particularly useful when dealing with JSON data, as it often contains nested arrays and objects. By using array functions, users can extract elements, transform array contents, and perform aggregations directly on complex data without needing to flatten the data structure into a relational format first, thereby preserving the original data context and simplifying the analysis of multi-dimensional data.

**Domain**

ELT With Spark SQL and Python

**Question 26Skipped**

A data analyst has developed a query that runs against Delta table. They want help from the data engineering team to implement a series of tests to ensure the data returned by the query is clean. However, the data engineering team uses Python for its tests rather than SQL. Which of the following operations could the data engineering team use to run the query and operate with the results in PySpark?

**SELECT \* FROM sales**

**There is no way to share data between PySpark and SQL.**

**spark.delta.table**

**spark.table**

**Correct answer**

**spark.sql**

Overall explanation

spark.sql, allows you to run SQL queries within a PySpark context, which includes accessing and querying Delta tables. This option is more versatile and directly supports running the original SQL query developed by the data analyst, making it a suitable choice for performing data quality tests on the Delta table.

**Domain**

ELT With Spark SQL and Python

**Question 27Skipped**

Consider the following Databricks Spark SQL code snippet. What does this code accomplish?

1.  CREATE OR REPLACE TEMPORARY VIEW daily\_sales AS
2.  SELECT date, SUM(amount) AS total\_sales
3.  FROM sales
4.  GROUP BY date;
5.  CREATE OR REPLACE GLOBAL TEMP VIEW global\_daily\_sales AS
6.  SELECT \* FROM daily\_sales;

**Creates two identical views, daily\_sales and global\_daily\_sales, both of which are accessible across multiple Spark sessions and applications.**

**Throws an error because a Global Temporary View cannot be created from a Temporary View.**

**Correct answer**

**Creates a Temporary View daily\_sales and a Global Temporary View global\_daily\_sales with the same data, but global\_daily\_sales can be accessed in any session within the application.**

**Creates a Materialized View daily\_sales that is stored on disk and a Global Temporary View global\_daily\_sales that is stored in memory.**

Overall explanation

The code first creates a session-scoped Temporary View daily\_sales aggregating sales by date. It then creates a Global Temporary View global\_daily\_sales based on the daily\_sales Temporary View, making the aggregated data accessible across different sessions within the same application.

**Domain**

ELT With Spark SQL and Python

**Question 28Skipped**

In Spark SQL, which of the following is necessary to perform a PIVOT operation?

**An aggregate function to summarize the data**

**A LIMIT clause to restrict the number of rows returned**

**Correct answer**

**A DISTINCT clause to ensure unique values**

**A JOIN clause to merge data from different tables**

Overall explanation

When using the PIVOT clause in Spark SQL, it is essential to specify an aggregate function. This function is used to summarize the data that will be transformed from rows into columns. The choice of aggregate function (e.g., SUM, AVG, MAX) depends on the type of summary or analysis required by the user.

**Domain**

ELT With Spark SQL and Python

**Question 29Skipped**

What prefix is used in Databricks SQL to query data directly from Parquet files?

**FILE**

**DATA**

**Correct answer**

**parquet**

**PARQUET**

Overall explanation

Databricks SQL, the prefix parquet is used after the FROM keyword to specify that the data source is a Parquet file. This allows users to perform SQL queries directly on Parquet files by providing the path to the file, leveraging Parquet's efficient columnar storage format for fast and flexible data analysis.

**Domain**

ELT With Spark SQL and Python

**Question 30Skipped**

A data engineer tries to remove a Spark SQL table named my\_table with the intention of erasing both the table's metadata and its data. They execute the command: DROP TABLE IF EXISTS my\_table; After running this command, my\_table no longer shows up in the list of tables, but the data files remain. What explains the persistence of the data files despite the deletion of the metadata?

**The table's data exceeded 10 GB**

**Correct answer**

**The table was set up as an external table.**

**The table was specified without a location.**

**The table was a managed table.**

**The table's data was under 10 GB.**

Overall explanation

In Spark SQL, when you drop a managed table, Spark deletes both the table's metadata and its data files. However, if the table is an external table, dropping it only removes the metadata, leaving the data files untouched. This behavior ensures that you don't accidentally lose data that might be shared with other tables or applications. The fact that the data files still exist after dropping the table suggests that it was defined as an external table, which is why only the metadata was deleted upon executing the DROP TABLE command. Managed tables, on the other hand, store both data and metadata within Spark's control, leading to both being deleted when the table is dropped.

**Domain**

ELT With Spark SQL and Python

**Question 31Skipped**

A data analyst has established a Delta table named sales that the entire data analysis team utilizes. They are seeking assistance from the data engineering team to conduct a series of tests to verify the cleanliness of the data. The data engineering team, however, prefers Python for their testing instead of SQL. Which command could the data engineering team use to access the sales table in PySpark?

**spark.delta.table("sales")**

**spark.sql("SELECT \* FROM sales")**

**SELECT \* FROM sales**

**Correct answer**

**spark.table("sales")**

**Data sharing between PySpark and SQL is not possible.**

Overall explanation

The correct way for the data engineering team to access a table in PySpark, regardless of whether it was created in SQL, is by using the spark.table("tableName") method. This command allows PySpark to read the table into a DataFrame, enabling the team to apply Python-based data manipulation and testing methods. This approach facilitates seamless integration between SQL and PySpark within the Databricks environment, allowing teams to leverage the strengths of both SQL and Python for data analysis and engineering tasks.

**Domain**

ELT With Spark SQL and Python

**Question 32Skipped**

Given a table named 'events' with a column 'items' that contains an array of values, which SQL query would transform each item of the array into a separate row?

1.  SELECT user\_id, event\_timestamp, event\_name, UNNEST(items) AS item
2.  FROM events;

**Correct answer**

1.  SELECT user\_id, event\_timestamp, event\_name, explode(items) AS item
2.  FROM events;
3.  SELECT user\_id, event\_timestamp, event\_name, ARRAY(items) AS item
4.  FROM events;
5.  SELECT user\_id, event\_timestamp, event\_name, items
6.  FROM events;

Overall explanation

SELECT user\_id, event\_timestamp, event\_name, explode(items) AS item FROM events;

**Domain**

ELT With Spark SQL and Python

**Question 33Skipped**

What syntax does Spark SQL use to query subfields within STRUCT data types?

**Semicolon (;)**

**Comma (,)**

**Correct answer**

**Dot (.)**

**Colon (:)**

Overall explanation

The dot (.) syntax is utilized in Spark SQL to access subfields of STRUCT types. STRUCT types are complex data types in Spark SQL that define a tree of nested fields, and the dot syntax allows for navigating this structure.

**Domain**

ELT With Spark SQL and Python

**Question 34Skipped**

Which of the following commands will return the number of null values in the member\_id column?

1.  SELECT count\_null(member\_id)
2.  FROM my\_table;
3.  SELECT null(member\_id)
4.  FROM my\_table;
5.  SELECT count(member\_id) - count\_null(member\_id)
6.  FROM my\_table;

**Correct answer**

1.  SELECT count\_if(member\_id IS NULL)
2.  FROM my\_table;
3.  SELECT count(member\_id)
4.  FROM my\_table;

Overall explanation

The count\_if function along with the condition member\_id IS NULL to count the number of null values in the member\_id column of the my\_table. This is the standard way to count null values in SQL queries.

**Domain**

ELT With Spark SQL and Python

**Question 35Skipped**

What SQL data type should be used to store an ordered collection of multiple names in a table?

**Correct answer**

**ARRAY**

**VARCHAR**

**MAP**

**STRUCT**

Overall explanation

**An ARRAY data type is suitable for storing an arbitrary number of elements of the same data type, which in this case could be a collection of names as strings.**

**Domain**

ELT With Spark SQL and Python

**Question 36Skipped**

A data engineer working in Databricks wants to create a User-Defined Function (UDF) to convert temperatures from Celsius to Fahrenheit for a dataset containing global weather information. Which of the following best describes the steps they should take to accomplish this within a Databricks notebook?

**Correct answer**

**Create a UDF in Databricks that applies the conversion formula and use it in a Spark SQL query to transform the temperature column.**

**Export the dataset to a CSV file, convert the temperatures using an external tool, and then re-import the dataset into Databricks.**

**Directly modify the dataset in the data lake to convert all temperatures to Fahrenheit before importing it into Databricks.**

**Use a built-in Spark SQL function that automatically converts temperatures from Celsius to Fahrenheit.**

Overall explanation

Creating a UDF in Databricks allows for custom transformations to be applied directly within Spark SQL queries, offering a flexible and efficient way to manipulate data. For the task of converting temperatures from Celsius to Fahrenheit, the UDF would encapsulate the conversion formula (Fahrenheit = Celsius \* 9/5 + 32). This method is more efficient and scalable within Databricks compared to manually modifying data or relying on external tools. Example code snippet for creating and using the UDF in Databricks: # Define the UDF in Python def celsiusToFahrenheit(celsius): return (celsius \* 9.0/5) + 32 # Register the UDF spark.udf.register("celsiusToFahrenheitUDF", celsiusToFahrenheit) # Example usage in Spark SQL spark.sql("SELECT city, celsiusToFahrenheitUDF(temperature) AS temperature\_fahrenheit FROM weather\_data") This approach leverages Databricks' capability to integrate PySpark and Spark SQL for data processing, enabling the data engineer to apply custom data transformations efficiently within their analytical workflows.

**Domain**

ELT With Spark SQL and Python

**Question 37Skipped**

Consider a column of JSON strings that needs to be converted to structs. Which SQL command creates a new table to store these structs with no null fields, based on a schema generated from an example JSON string?

**Correct answer**

1.  CREATE OR REPLACE TABLE parsed\_events AS
2.  SELECT FROM\_JSON(json\_column, schema\_of\_json('<example\_json>')) AS struct\_column
3.  FROM original\_table;
4.  CREATE TABLE parsed\_data AS
5.  SELECT TO\_STRUCT(json\_column)
6.  FROM original\_table;
7.  CREATE TABLE parsed\_data AS SELECT CAST(json\_column AS STRUCT)
8.  FROM original\_table;
9.  CREATE TABLE parsed\_data AS SELECT FROM\_JSON(json\_column) AS struct\_column
10.  FROM original\_table;

Overall explanation

1.  CREATE OR REPLACE TABLE parsed\_events AS
2.  SELECT FROM\_JSON(json\_column, schema\_of\_json('<example\_json>')) AS struct\_column
3.  FROM original\_table;

**Domain**

ELT With Spark SQL and Python

**Question 38Skipped**

A data engineer wants to create a relational object by pulling data from two tables. The relational object does not need to be used by other data engineers in other sessions. In order to save on storage costs, the data engineer wants to avoid copying and storing physical data. Which of the following relational objects should the data engineer create?

**Database**

**View**

**Spark SQL Table**

**Delta Table**

**Correct answer**

**Temporary View**

Overall explanation

A temporary view is the appropriate choice because it allows the data engineer to combine data from two tables without saving it permanently. Temporary views are only available for the duration of the current session, ensuring that the mixed data is not stored long-term and doesn't consume unnecessary storage space.

**Domain**

ELT With Spark SQL and Python

**Question 39Skipped**

Which data type is typically involved when using the explode function?

**STRING**

**BOOLEAN**

**INTEGER**

**Correct answer**

**ARRAY**

Overall explanation

The explode function operates on ARRAY data types since it is designed to take each element within an array and create a new row for it in the result set.

**Domain**

ELT With Spark SQL and Python

**Question 40Skipped**

When creating a table in Databricks using a JDBC connection, which of the following SQL commands correctly attempts to create a new table and throws an error if the table with that name already exists?

**Correct answer**

1.  CREATE TABLE new\_table
2.  USING jdbc
3.  OPTIONS (
4.  url 'jdbc:mysql://database.example.com:3306/mydb',
5.  dbtable 'new\_table',
6.  user 'username',
7.  password 'password'
8.  )
9.  CREATE TABLE IF NOT EXISTS new\_table
10.  USING jdbc
11.  OPTIONS (
12.  url 'jdbc:mysql://database.example.com:3306/mydb',
13.  dbtable 'existing\_table',
14.  user 'username',
15.  password 'password'
16.  )
17.  INSERT INTO new\_table
18.  SELECT \* FROM jdbc('jdbc:mysql://database.example.com:3306/mydb', 'existing\_table', 'username', 'password')
19.  CREATE TABLE new\_table AS
20.  SELECT \* FROM jdbc('jdbc:mysql://database.example.com:3306/mydb', 'table\_name', 'username', 'password')

Overall explanation

This SQL command for creating a new table through a JDBC connection in Databricks specifies the JDBC connection details and attempts to create a new table named new\_table. If a table with that name already exists, Databricks will throw an error, which aligns with the default behavior for creating tables via JDBC connections.

**Domain**

ELT With Spark SQL and Python

**Question 41Skipped**

A data engineer needs to run the last part of a Python script only if the variable day\_of\_week is 1 and review\_period is True. Which line of code should they use to start this part of the script?

**if day\_of\_week == 1 and review\_period == "True"**

**Correct answer**

**if day\_of\_week == 1 and review\_period**

**if day\_of\_week = 1 and review\_period = "True"**

**if day\_of\_week = 1 & review\_period: = "True"**

**if day\_of\_week = 1 and review\_period**

Overall explanation

The correct line of code checks if day\_of\_week equals 1 using double equals == and also checks if review\_period is true without comparing it to the string "True". Why?: In Python, to compare values, you should use ==, not =, because = is used for assigning values. The correct option does this by using == to compare day\_of\_week to 1. It also simply checks if review\_period is true, which is the right way to check a boolean (true or false) value in Python. Some incorrect options try to compare a boolean to a string or use assignment operators in a condition, which is not how you write conditions in Python. The best choice directly and correctly checks both conditions without unnecessary comparisons or syntax errors.

**Domain**

ELT With Spark SQL and Python

**Question 42Skipped**

Which statement correctly describes the difference between a Temporary View and a Global Temporary View?

**Correct answer**

**Temporary Views are session-scoped and cannot be accessed by other Spark sessions, whereas Global Temporary Views are accessible across multiple Spark sessions within the same Spark application.**

**Temporary Views are stored in the Hive metastore and are accessible across all sessions, whereas Global Temporary Views are session-scoped and not stored in the Hive metastore.**

**There is no difference; both Temporary and Global Temporary Views have the same scope and visibility across Spark sessions.**

**Temporary Views can be created on disk, making them persistent across Spark sessions, whereas Global Temporary Views are in-memory only and lost after the session ends.**

Overall explanation

Temporary Views are tied to a single Spark session and are not visible to other sessions, while Global Temporary Views are visible across all sessions of a Spark application but are not stored in the Hive metastore.

**Domain**

ELT With Spark SQL and Python

**Question 43Skipped**

If you need to associate a numerical value with each key in a SQL table, which data type would be most appropriate?

**INTEGER**

**Correct answer**

**MAP**

**ARRAY**

**STRUCT**

Overall explanation

A MAP data type consists of key-value pairs where each key is associated with one value, making it ideal for scenarios where a numerical value needs to be tied to a specific key.

**Domain**

ELT With Spark SQL and Python

**Question 44Skipped**

A data engineer needs to normalize text data within a Databricks notebook by creating a User-Defined Function (UDF) that converts all strings to lowercase and removes spaces. Which approach should they take to achieve this within Databricks?

**Utilize a Databricks built-in function for string normalization in a Spark SQL query.**

**Correct answer**

**Implement a UDF in Databricks that lowercases and trims spaces from strings, applying it via a Spark SQL query.**

**Manually edit each string in the dataset before loading it into Databricks.**

**Export the dataset, process it with a script outside of Databricks, and then re-import the cleaned data.**

Overall explanation

Implementing a User-Defined Function (UDF) in Databricks to perform specific string manipulations offers a direct and scalable solution within the Spark SQL environment. This method allows for custom data cleaning operations, such as converting strings to lowercase and removing spaces, to be encapsulated within a UDF and applied across datasets dynamically within SQL queries. This approach avoids the need for external data manipulation or manual preprocessing, streamlining data processing workflows directly within Databricks.

**Domain**

ELT With Spark SQL and Python

**Question 45Skipped**

How do you create a permanent view in Databricks SQL that references data from a CSV file stored in DBFS?

**Permanent views cannot reference CSV files directly; they must be loaded into a table first.**

**Use the CREATE VIEW statement with the USING csv option followed by the path to the CSV file.**

**Use the CREATE PERMANENT VIEW statement and specify the CSV file path directly after the FROM keyword.**

**Correct answer**

**Use the CREATE VIEW statement followed by a SELECT query that uses the csv prefix to reference the CSV file.**

Overall explanation

In Databricks SQL, you can create a permanent view by using the CREATE VIEW statement followed by a SELECT query. To reference a CSV file directly, you use the csv prefix after the FROM keyword in your SELECT query within the view definition. This allows you to create a view that directly references and queries data from a CSV file stored in DBFS.

**Domain**

ELT With Spark SQL and Python